# GSD Runtime Agents

This directory is an active tooling-owned runtime surface for GSD only.

It is not a repo-specific workflow-control layer. Legacy hand-built role definitions were archived to `docs/archive/workflow-legacy/codex/agents/`.
