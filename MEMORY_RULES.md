# MEMORY_RULES.md

> Legacy reference only. This path no longer defines the active repository workflow.

Use `GSD`, local `.planning/` artifacts, and `docs/canonical/README.md` for current work.

Archived historical memory-policy text moved to `docs/archive/workflow-legacy/wrappers/MEMORY_RULES.md`.
