# MEMORY_RULES.md

> Legacy reference only. This path no longer defines the active repository workflow.

Use `GSD`, `.planning/README.md`, local `.planning/` artifacts, and `docs/canonical/README.md` for current work.

Archived historical memory-policy text moved to `docs/archive/workflow-legacy/wrappers/MEMORY_RULES.md`.
Use `docs/archive/workflow-legacy/README.md` only for historical workflow lookup.
