# TASK-2026-03-31-005

## Task type

- `analysis`
- 本轮目标是把 `axis + side + support` 主线下的“弱耦合结构路线”收敛成唯一优先的正式方案，并定义清晰的 implementation boundary；本轮不写代码、不重训。

## Goal

- 在当前正式主线 `axis + side + support` 语境下，正式确定一条唯一优先的结构路线：保留已被 `support-only = 74.6` 证明有效的 support 主收益源，同时切断 axis / side 几何分支对 shared backbone / semantic 主路的直接梯度拖拽风险。

## Why now

- `TASK-2026-03-31-004` 已经完成分析收束，确认了真实性能问题、事实 / 假设边界，以及 active 主线下的真实耦合点。
- 当前阶段正式核心是 `axis + side + support` rollout / verification，而不是回到旧 `support + dir + dist` 主线，也不是继续扫 support 参数。
- 当前最需要优先回答的问题已经从“是不是口径问题”切换为“在保留 support 收益的前提下，哪一种最小结构改动能避免 axis / side 拖拽 semantic”。

## In scope

- 在 `axis + side + support` 语境下，把已确认事实与结构决策假设继续分开表述。
- 基于已定点的真实耦合点，正式选择唯一优先的结构原则，而不是保留多个并列主方案。
- 说明为什么该路线优先于调权重、加阈值、两阶段训练、或更大范围的 branch/backbone 重构。
- 给出后续实现轮可遵循的最小 implementation boundary 与最小 validation plan。

## Out of scope

- 不修改业务代码、模型代码、loss、trainer、Pointcept 或数据管线。
- 不运行训练、不补新实验、不追加 smoke / full-train。
- 不回到旧 `support + dir + dist` 主线。
- 不同时保留多个并列主方案。
- 不扩展到 Pointcept 级重构、训练策略重写、test/export/visualization。

## Constraints

- 唯一允许主动维护的项目根是 `sbf-net`；`Pointcept` 默认只读。
- 当前 workflow smoke 的已确认事实仍是上一轮 `TASK-2026-03-31-004` 链路的 `WARN / 1 stale`；不能写成 `PASS`。
- 当前正式主线必须表述为 `axis + side + support`。
- 旧 `support-shape` full-train 只能作为失败旁证，不能与当前 active rollout 混写。
- `dist` 相关表述必须继续降格为“旧失败 run 中 dist 诊断值改善，但它不是该轮主训练目标”。
- 结论中必须继续区分：
  - 已确认事实
  - 结构决策假设
  - 选定路线
- 本轮必须收敛为单一路线，不能输出多个并列主方案。

## Read first

- `AGENTS.md`
- `project_memory/current_state.md`
- `project_memory/tasks/TASK-2026-03-31-004.md`
- `reports/round_updates/TASK-2026-03-31-005.codex.candidate_brief.draft.md`
- `reports/log_summaries/semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train.summary.md`

## Implementation plan

1. 沿用 `TASK-004` 已收束的事实、假设边界和 active coupling 定点结果，不重新整仓接手。
2. 在当前 active 主线下正式锁定唯一优先的结构路线，并明确其针对的是哪几个真实耦合点。
3. 与其他候选路线做单轮比较，解释为什么本轮不优先选“继续调权重 / 加阈值 / 两阶段训练 / 更大范围解耦”。
4. 给出后续实现轮的最小文件边界、最小验证口径和明确 non-goals。
5. 输出正式分析 brief；本轮不进入代码改动。

## Checkpoints

- 当前 formal analysis task 文件：`project_memory/tasks/TASK-2026-03-31-005.md`
- 候选来源 artifact：`reports/round_updates/TASK-2026-03-31-005.codex.candidate_brief.draft.md`
- 上一轮已收束结果：`project_memory/tasks/TASK-2026-03-31-004.md`

## Validation

- 结论必须明确引用并承认以下已确认事实：
  - best scalar `val_mIoU = 0.7316 (=73.16)`
  - latest scalar `val_mIoU = 0.7085`
  - `support-only baseline = 74.6`
- 结论必须把“已确认事实”和“结构决策假设”分开写。
- 结论必须只保留一个正式主方案，而不是多个并列方案。
- 结论必须明确说明为什么该路线优先于至少以下替代项：
  - 继续调 axis / side / support loss 权重
  - 仅增加 support 阈值 / gating
  - 先做分阶段 / 两阶段训练
  - 更大范围的 backbone / branch 重构
- 结论必须给出后续实现轮的最小 implementation boundary 与 validation plan。
- 本轮 validation 只基于现有 task 文档、candidate brief、摘要证据和已定点的 active coupling，不要求新训练。

## Done condition

- 已形成 `TASK-005` 的正式分析 brief。
- 已正式锁定唯一优先的结构原则。
- 已明确“为什么是它，而不是其他路线”。
- 已写清后续实现轮的最小 implementation boundary 与 validation plan。
- 本轮全程未进入实现、未写代码、未跑训练。

## Review / acceptance plan

- 由作者按以下标准验收：
  - 是否仍严格锚定 `axis + side + support`
  - 是否把事实与假设分开
  - 是否只保留单一路线
  - implementation boundary 是否足够小而清晰
  - 是否没有重新滑回旧 `support + dir + dist` 主线

## Deliverables

- 一份 `TASK-005` 的正式分析版 task brief。
- 一条唯一优先的 `Chosen Structural Principle`。
- 一份 `Why this over alternatives` 解释。
- 一份明确的 `Implementation Boundary`。
- 一份最小 `Validation Plan`。
- 一份 `Non-goals / Risks`。

## Closeout expectation

- 若本轮分析被作者接受，后续实现轮必须以本 task 作为唯一入口，而不是重新回到 candidate brief 或 `TASK-004` 的讨论文本。
- 本轮若只完成 formal analysis 而未进入实现，不需要写入实现结果，但需要保持 `current_state` 与 active task 指针同步。

## Result

- 当前状态：active，formal analysis in progress。
- 已确认事实：`TASK-004` 已证明当前 full-train best scalar `val_mIoU = 0.7316 (=73.16)` 低于 canonical `support-only = 74.6`，且当前 active 主线仍是 `axis + side + support`。
- 已确认事实：active 主线下已定点的真实耦合点包括共享 backbone、`SupportConditionedEdgeHead` 中 support 处于 axis / side 上游、`loss_semantic + loss_edge` 直接相加、以及 trainer 对聚合后单个 `loss` 的一次反传。
- 结构决策假设：问题最像 axis / side 几何损失通过当前耦合路径直接拖拽 shared backbone / semantic 主路，而不是 support 本身的收益无效。
- 当前选定路线：采用“support 保留耦合、axis / side stop-grad 私有化”的单一路线，后续实现轮只围绕这一原则落地。

## Next step

- 在不写代码的前提下，把该单一路线进一步表述为可直接执行的后续实现边界与验证计划。
- 若作者认可本 formal brief，再新开实现轮；实现轮不得重新回到多方案并列状态。
