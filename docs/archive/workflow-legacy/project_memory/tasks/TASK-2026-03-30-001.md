# TASK-2026-03-30-001

## Goal

- 在 CUDA-enabled `ptv3` 环境中确认当前 active 主线 `axis + side + support` 的 smoke 训练链路可完整跑通，并为是否进入 full train 提供可确认结论。

## Why now

- 当前阶段是 `Stage-2 architecture rollout / verification phase`，本轮 task 的作用是把已落地的 `axis-side` 主线核证成可确认的 smoke 路线。
- 只有先完成 smoke 核证，下一步才有资格进入 `axis-side` full train。

## In scope

- 使用 `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-axis-side-train-smoke.py` 对当前 `axis-side` 主线做最小 smoke 验证。
- 确认 train 2 epoch + validate 1 epoch 是否完整跑通。
- 核对日志中是否出现 axis-side 相关 train / val 指标，以及是否生成 checkpoint。
- 若失败，定位失败位置属于环境问题、宿主接口问题，还是项目内实现问题，并形成最小问题说明。

## Out of scope

- 不运行 `axis-side` full train。
- 不重新扫 support 参数，不重开 `dist` 主线。
- 不修改 `scripts/train/train.py`、主配置 `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-train.py`、Pointcept 主体或训练逻辑。
- 不顺手改 Route A、`B′`、test/export/visualization。

## Constraints

- 唯一允许主动维护的项目根是 `semantic-boundary-field`。
- `Pointcept` 只是宿主依赖，默认只读；若问题疑似来自宿主接口，必须停止并汇报。
- 当前阶段优先暴露问题，不允许为了跑通加入 fallback、默认兼容层、自动绕过或吞错。
- 结果中必须区分“作者已确认事实”“当前 workspace 可直接复核产物”“仅代码已落地状态”。

## Read first

- `AGENTS.md`
- `project_memory/current_state.md`
- `project_memory/01_current_architecture.md`
- `project_memory/02_loss_design.md`
- `project_memory/04_training_rules.md`

## Implementation plan

1. 先生成或读取当前 `codex` target 的 context packet，再确认 smoke 使用的环境、配置和数据前提是否满足：`conda run -n ptv3`、`samples` 数据、`axis-side` smoke config。
2. 运行或复核一次 `axis-side` smoke，并先为对应日志生成 `reports/log_summaries/` 摘要，再记录 train / val 是否完整出现。
3. 先检查摘要中的最后值、最佳值、异常与自动问题；只有摘要不足时再下钻原始 `train.log`，并确认输出目录中是否生成 `model_best.pth`、`model_last.pth`。
4. 若 smoke 失败，最小定位失败位置，并判断是否触及 Pointcept / 宿主边界。
5. 一轮结束时或证据刷新后，优先运行 `scripts/agent/refresh_round_artifacts.py` 顺序刷新 summary、context packet 和 round update；只有在需要单独调试 fixed-scope preview/apply 时，再直接调用 `scripts/agent/update_round_artifacts.py`。

## Validation

- 日志摘要中能确认完整的 train 记录和至少一次 val 记录。
- 输出目录中存在 `train.log`、`model_best.pth`、`model_last.pth`。
- 摘要或必要的原始日志片段能看到 `axis-side` 路线对应的边界指标或 trainer summary 分支，而不是仅有启动信息。
- 若失败，必须能指出失败阶段和直接证据，不能只写“未通过”。

## Deliverables

- 一份可直接引用的 smoke 结果结论：通过 / 未通过 / 被环境阻塞。
- 对应的 `*.summary.md` 和 `*.summary.json`。
- 对应日志或输出产物位置。
- 若未通过，一份最小失败归因说明。

## Result

- 当前状态：已核证。
- 根据 `reports/log_summaries/semantic_boundary_axis_side_train_smoke_train.summary.{md,json}`，`axis-side` smoke 的最后一个有效 session 为 `validated_with_checkpoints`，device `cuda`，并在 `outputs/semantic_boundary_axis_side_train_smoke/model/` 下生成 `model_best.pth` 与 `model_last.pth`。
- 同一 `train.log` 中共检测到 `4` 次启动 / 会话；当前以最后一个有效 session 作为本轮 smoke 结论。
- 指标快照：latest / best val `mIoU=0.0378`。
- 待继续分析：为何同一日志中出现 `4` 次启动，以及当前 `val_mIoU=0.0378` 是否指向 config / data / label mismatch。

## Next step

- 下一轮应新建 `axis-side` full train 验证 task，并保留当前 smoke 摘要作为准入证据。
- 在 full train 前先解释当前 smoke 的低 `val_mIoU` 与 `4` 次启动记录。
- 开新窗口前重新生成 context packet 与 round update draft，避免继续沿用旧状态。
