# Context Packet: TASK-2026-03-31-003 / web_chat

- Generated at: `2026-03-31T07:11:16Z`
- Target: `web_chat`
- Current state source: `project_memory/current_state.md`
- Task source: `project_memory/tasks/TASK-2026-03-31-003.md`

## Purpose

- Provide a compact packet that can be pasted or attached to a new web chat without manually restating long context.
- This packet is excerpt-oriented. Canonical facts still live in `AGENTS.md`, `project_memory/current_state.md`, the current task file, and linked summaries.

## Read Order

1. This packet
2. AGENTS.md for global rules
3. project_memory/current_state.md for current facts
4. Current task file for this round
5. Only the linked docs or log summaries that remain necessary

## Current State Snapshot

- Current task: 当前单轮执行中心：`project_memory/tasks/TASK-2026-03-31-003.md`
- 唯一允许主动维护的项目根是 `sbf-net`；`Pointcept` 只是宿主依赖，默认只读。
- 当前阶段状态是 `Stage-2 architecture rollout / verification phase`。
- 当前 active 主线统一为 `axis + side + support`；作者口头中的 `magnitude` 在当前代码 / 文档同步中等价于 `support`。
- 当前分析中心已切到 `semseg-pt-v3m1-0-base-bf-edge-support-shape-train` 的 full train 结果；`axis-side` smoke 作为前置准入证据保留。
- `semantic-only baseline = 73.8`。
- `support-only(reg=1, cover=0.2) = 74.6`，当前最佳。
- `support + dir + dist = 71`，是旧 signed-direction 结构下的失败事实。
- `axis-side` 的 loss / evaluator / trainer / config 修改都已在当前 working tree 中落地。
- 当前 `axis-side` smoke 已有一份可直接复核的摘要证据：最后一个有效 session 为 `validated_with_checkpoints`，device `cuda`，并在 `/home/mty/Python_Proj/for_build_seg/Pointcept/sbf-net/outputs/semantic_boundary_support_shape_train/model/` 下生成 `mode...
- 第一优先级：新建 `TASK-004`，在干净口径下分析 full-train `val_mIoU = 0.7316` 低于 baseline（74.6）的原因，判断是模型层、超参还是训练策略问题。
- 若任务需要补充上下文，只按需读取一个专题文件：

## Current Task Snapshot

- Goal: 修正 full-train 分析链中的“验证口径与产物绑定”问题，使当前权威口径稳定收敛到 semantic scalar `val_mIoU`，并消除 summary / packet / workflow smoke / round artifact 之间的指标混淆与 primary summary conflict。
- Why now: `TASK-2026-03-30-002` 已把当前 full-train 的主要问题收敛到“验证口径问题 + 记录/打包问题”为主，而不是模型主线或 loss 主线先失效。
- Why now: 若当前仍混用 step 级 `mIoU` 与 scalar `val_mIoU`，并且 packet / smoke / round draft 的 primary summary source 不一致，那么任何后续模型判断都会先被记录层噪声污染。
- In scope: 明确 trainer / evaluator 的验证指标命名与输出边界，确保 semantic 主权威指标口径清楚指向 scalar `val_mIoU`。
- In scope: 修正日志摘要层对 step 级指标与 scalar 级指标的区分与展示。
- Constraints: 唯一允许主动维护的项目根是 `sbf-net`。
- Constraints: `Pointcept` 只是宿主依赖，默认只读；若问题疑似来自宿主接口，必须停止并汇报。
- Validation: 基于现有 `outputs/semseg-pt-v3m1-0-base-bf-edge-support-shape-train/train.log` 即可完成复核，不要求新训练。
- Validation: 更新后的摘要能明确区分：
- Next step: 下一轮应新建 `TASK-004`，在干净口径下判断当前 full-train `val_mIoU = 0.7316` 低于 baseline 的原因是模型层、超参还是训练策略问题。
- Next step: 当前 smoke / packet / summary 链路已对齐，可直接作为下一轮的输入证据。

## Related Files

### Core Docs

- `AGENTS.md` -> `AGENTS.md`
- `Current state` -> `project_memory/current_state.md`
- `Current task` -> `project_memory/tasks/TASK-2026-03-31-003.md`
- `project_memory/tasks/TASK-2026-03-30-002.md` -> 解读 `semseg-pt-v3m1-0-base-bf-edge-support-shape-train` 的 full train 结果，提炼关键现象与异常点，并判断下一轮是否需要新的实现改动。 | `TASK-2026-03-30-001` 已完成 `axis-side` smoke 核证；继续沿用它会把“smoke 是否可跑通”和“full train 结果如何解释”混成同一轮任务。
- `docs/validation_design.md` -> 与训练 loss 保持一致 | 指标具备可解释性
- `reports/log_summaries/semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train.summary.md` -> File: `/home/mty0201/Pointcept/sbf-net/outputs/semseg-pt-v3m1-0-base-bf-edge-support-shape-train/train.log` | Size: `42.92 MB` (45006615 bytes)
- `reports/workflow_smokes/TASK-2026-03-30-002.codex.workflow_consistency_smoke.md` -> Generated at: `2026-03-30T22:34:34Z` | Target scope: `codex`
- `project_memory/05_active_stage.md` -> 先在 CUDA-enabled `ptv3` 环境确认 `axis-side` smoke；当前目标是把已落地主线核证成可确认 smoke 路线。 | 仅在 `axis-side` smoke 确认通过后，再安排 `axis-side` full train。
- `project_memory/90_archived_decisions.md` -> 历史 edge loss 主线并非无效尝试: 早期设计曾带来约 `+1` mIoU 收益；其中 `reg` 思路与当前 support_reg 本质一致，`overlap` 本质上就是当前的覆盖约束；当时尚未拆成 `direction / distance`，而是直接预测连续位移向量场 `vec`。该历史结论只作为当前阶段判断的背景，不改变当前“已进入 `Stage-2 architecture rollout / verific... | 继续把旧 signed-direction 表达 `support + dir + dist` 当作当前 active 主表达: 已废弃；它现在只作为历史对照和已确认失败路线存在，当前 active 主线是 `axis + side + support`。

### Module Map

- `Model`: `SharedBackboneSemanticBoundaryModel` -> `project/models/semantic_boundary_model.py` (Resolved from architecture symbol search.)
- `Model`: `SupportConditionedEdgeHead` -> `project/models/semantic_boundary_model.py` (Resolved from architecture symbol search.)
- `General`: `SupportConditionedEdgeHead` -> `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-stage2-v2-model.py` (Resolved from architecture symbol search.)
- `Dataset`: `BFDataset` -> `project/datasets/bf.py` (Resolved from architecture symbol search.)
- `Transform`: `InjectIndexValidKeys` -> `project/transforms/index_keys.py` (Resolved from architecture symbol search.)
- `Loss`: `AxisSideSemanticBoundaryLoss` -> `project/losses/axis_side_loss.py` (Resolved from architecture symbol search.)
- `Loss`: `SemanticBoundaryLoss` -> `project/losses/semantic_boundary_loss.py` (Resolved from architecture symbol search.)
- `Loss`: `RouteASemanticBoundaryLoss` -> `project/losses/route_a_loss.py` (Resolved from architecture symbol search.)
- `Evaluator`: `AxisSideEvaluator` -> `project/evaluator/axis_side_evaluator.py` (Resolved from architecture symbol search.)
- `Evaluator`: `SemanticBoundaryEvaluator` -> `project/evaluator/semantic_boundary_evaluator.py` (Resolved from architecture symbol search.)
- `Runtime`: `SemanticBoundaryTrainer` -> `project/trainer/trainer.py` (Resolved from architecture symbol search.)

## Training Evidence

- Summary `semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train` -> `reports/log_summaries/semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train.summary.md`, `reports/log_summaries/semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train.summary.json`
  - Source `outputs/semseg-pt-v3m1-0-base-bf-edge-support-shape-train/train.log`; runs `1`, epoch range `1` -> `100`.
  - Latest session status `validated_with_checkpoints`, device `cuda`, checkpoints `314`.
  - Latest val_mIoU `0.7085` (epoch-aggregated scalar).
  - Best val_mIoU `0.7316` (epoch-aggregated scalar).
- Summary `semantic_boundary_axis_side_train_smoke_train` -> `reports/log_summaries/semantic_boundary_axis_side_train_smoke_train.summary.md`, `reports/log_summaries/semantic_boundary_axis_side_train_smoke_train.summary.json`
  - Source `outputs/semantic_boundary_axis_side_train_smoke/train.log`; runs `4`, epoch range `1` -> `1`.
  - Latest session status `validated_with_checkpoints`, device `cuda`, checkpoints `4`.
  - Latest val_mIoU `0.0378` (epoch-aggregated scalar).
  - Best val_mIoU `0.0378` (epoch-aggregated scalar).
  - Auto question: Why were there 4 startup attempts in the same log, and which session should be treated as authoritative?
  - Auto question: Is the latest val_mIoU 0.0378 expected for this run, or does it point to a config / data / label mismatch?

## Notes For This Target

- Favor short excerpts and explicit file paths that can be copied into a web chat if needed.
- Do not dump raw logs; use linked summaries first and only quote small log fragments if the summary is insufficient.
