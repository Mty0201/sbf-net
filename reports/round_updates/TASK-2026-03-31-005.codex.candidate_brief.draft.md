# Candidate Brief Draft: TASK-2026-03-31-005 / codex

- Status: `candidate only`
- Activation: `not activated`
- Execution mode: `no implementation`
- Parent decision thread: `project_memory/tasks/TASK-2026-03-31-004.md`

## Goal

- 基于已确认的 full-train 证据和 active 模块耦合点扫描，收敛出一条与当前正式主线 `axis + side + support` 一致的唯一优先结构路线。
- 本 brief 只用于“是否升格”的决策，不触发实现。

## Why now

- 当前权威口径已经足够说明这是一个真实性能问题：best scalar `val_mIoU = 0.7316 (=73.16)`，latest `0.7085`，低于 canonical `support-only = 74.6`。
- 当前 active 主线下的真实耦合点已经完成定点扫描，下一步需要先决定结构原则，而不是直接进入代码实现。
- 旧 `support-shape` full-train 只能作为失败旁证；它支持“方向类几何信号可能继续拖拽 semantic”这一怀疑，但不能被混写成当前 active rollout 已被 full-train 直接验证。

## In scope

- 明确区分已确认事实、待解释问题和当前最强假设。
- 用当前 `axis + side + support` 主线语言重述旧失败旁证。
- 基于已定点的真实 coupling 落点，给出唯一优先的 candidate principle。
- 定义后续若升格为正式 task 时的最小实现边界与最小验证口径。

## Out of scope

- 不激活 `TASK-2026-03-31-005`。
- 不改代码。
- 不开始实现。
- 不重训。
- 不重开旧 `support + dir + dist` 主线。
- 不扩展到 Pointcept 级重构。

## Constraints

- 当前 workflow smoke 事实必须写为 `WARN`，且仅 `1` 个 stale，不能写成 `PASS`。
- 当前正式主线必须写为 `axis + side + support`。
- 旧 `support-shape` full-train 只能作为旁证，不能与当前 active rollout 混写。
- `dist` 必须降格写为“旧失败 run 中 dist 诊断值改善，但它不是该轮主训练目标”。
- 不把当前最强假设写成已证实事实。

## Candidate Principle

- 保留 support 对 boundary branch 的直接监督，但 axis/side 只能在 support-conditioned 私有分支上学习，其损失不再直接回传 shared backbone。

## Candidate Implementation Boundary

- 最小候选实现边界优先限制在：
- `project/models/semantic_boundary_model.py`
- `project/models/heads.py`
- `project/losses/axis_side_loss.py`
- `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-axis-side-train.py`

## Candidate Validation

- 后续若升格并进入实现，先验证 axis-side smoke 的 schema / metric 完整性。
- full-train 仍以 scalar `val_mIoU` 为主口径。
- 最低对照基线继续使用 `support-only = 74.6`。

## Promotion Gate

- 只有在作者明确说“现在可以升格”之后，才允许把本文件迁移或改写为正式 task。
