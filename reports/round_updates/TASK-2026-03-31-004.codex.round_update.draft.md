# Round Update Draft: TASK-2026-03-31-004 / codex

- Mode: `draft`
- Target: `codex`
- Status: `analysis consolidation only`
- Current state source: `project_memory/current_state.md`
- Task source: `project_memory/tasks/TASK-2026-03-31-004.md`
- Primary summary: `reports/log_summaries/semseg-pt-v3m1-0-base-bf-edge-support-shape-train_train.summary.md`
- Workflow smoke fact: `WARN`, `1` stale, no summary-layer conflict

## Final Discussion Handoff

- 当前仍处于 `TASK-2026-03-31-004` 的分析决策收束轮；不进入实现，不激活新 task。
- 当前 workflow smoke 的事实必须写为 `WARN`，且仅 `1` 个 stale（`handoff/latest_round.md` 未同步），不能写成 `PASS`。
- 已确认的性能事实：best scalar `val_mIoU = 0.7316 (=73.16)`，latest `0.7085`，低于 canonical `support-only = 74.6`。
- 当前正式主线仍是 `axis + side + support`；旧 `support-shape` full-train 只能作为失败旁证，不能与当前 active rollout 混写。
- 旧失败 run 中方向类诊断值表现很差；`support` 指标继续改善；`dist` 只应降格写为“旧失败 run 中诊断值改善，但它不是该轮主训练目标”。
- 当前最强假设是“方向类几何信号引入后的多任务耦合失衡”，但它仍是最强假设，不是已证实事实。
- 当前唯一优先的 candidate principle：保留 support 对 boundary branch 的直接监督，但 axis/side 只能在 support-conditioned 私有分支上学习，其损失不再直接回传 shared backbone。

## Hypothesis Matrix

| Claim | Status | Note |
| --- | --- | --- |
| full-train 低于 baseline 是真实性能问题 | supported | 以 scalar `val_mIoU` 为权威口径，best `0.7316` 低于 `74.6` |
| 旧失败 run 中方向类信号很差 | supported | `val_loss_dir` / `dir_cosine` 的较优点停在 epoch 1 一带 |
| 旧失败 run 中 support 继续改善 | supported | support 指标较优点晚于 semantic best epoch |
| “dist 仍在学习” | downgraded | 只能写成旧失败 run 中 dist 诊断值改善；它不是该轮主训练目标 |
| 当前首因是几何分支多任务拖拽 | strongest hypothesis | 有 active 主线耦合点支持，但尚未被当前证据直接证明 |
| 下一轮应优先走弱耦合结构路线 | candidate conclusion | 已足够进入是否升格的讨论，但不等于已进入实现 |

## Candidate Implementation Boundary

- 已定点的真实耦合点一：`project/models/semantic_boundary_model.py` 中共享 backbone 特征同时供 semantic / boundary 分支使用。
- 已定点的真实耦合点二：`project/models/heads.py` 中 `SupportConditionedEdgeHead` 让 support 特征处于 axis/side 上游。
- 已定点的真实耦合点三：`project/losses/axis_side_loss.py` 中 `loss_semantic + loss_edge` 直接相加，且 `loss_edge = support + axis + side`。
- 已定点的真实耦合点四：`project/trainer/trainer.py` 对聚合后的单个 `loss` 做一次 backward / optimizer step，没有任务级梯度边界。
- 后续若进入实现，最小文件边界优先限制在 `semantic_boundary_model.py`、`heads.py`、`axis_side_loss.py`、`semseg-pt-v3m1-0-base-bf-edge-axis-side-train.py`。
- 最小验证口径仍是：先过 axis-side smoke 的 schema / metric 完整性，再看 full-train scalar `val_mIoU`；最低对照基线仍是 `74.6`。

## Candidate Task Brief

- Candidate artifact: `reports/round_updates/TASK-2026-03-31-005.codex.candidate_brief.draft.md`
- Status: `candidate only`
- Activation: `not activated`
- Execution mode: `no implementation`
- Goal: 在不写代码、不重训的前提下，把 active `axis + side + support` 主线下的真实耦合点说清，并收敛出唯一优先的弱耦合结构原则。
- Promotion gate: 只有作者明确说“现在可以升格”后，才允许把该 candidate brief 转成正式 task。

## Files Touched / Wording Aligned

- `project_memory/tasks/TASK-2026-03-31-004.md`
  - 已补写分析结论、真实耦合点、candidate principle 和 candidate brief 指针。
- `project_memory/current_state.md`
  - 已对齐当前 smoke 事实为 `WARN / 1 stale`，并明确 `support-shape` 仅为旁证。
- `reports/round_updates/TASK-2026-03-31-005.codex.candidate_brief.draft.md`
  - 新增 candidate-only brief artifact；未激活，不进入实现。
