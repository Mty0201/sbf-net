---
phase: 08-local-validation-and-experiment-handoff
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/canonical/sbf_validation_and_experiment_handoff.md
  - docs/canonical/sbf_semantic_first_route.md
  - docs/canonical/sbf_training_guardrails.md
autonomous: true
requirements: [VAL-02, COMP-04]

must_haves:
  truths:
    - "The milestone records the correct evidence boundary between local validation and full-train claims"
    - "The next full-train experiment direction is explicit and grounded in the modified route"
    - "The comparison baseline (support-only 74.6) is documented as the target to beat"
    - "No documentation overstates local smoke checks as full-train validation"
    - "Soft masking ablation direction is documented as top priority"
    - "Negative sample calibration risk is documented"
    - "Alpha-sigma coupling is recorded for future investigation"
    - "Adaptive inference route is documented as direction only"
  artifacts:
    - path: "docs/canonical/sbf_validation_and_experiment_handoff.md"
      provides: "Validation results, evidence limits, and experiment directions"
      contains: "Evidence Boundary"
    - path: "docs/canonical/sbf_semantic_first_route.md"
      provides: "Updated route doc with validation status"
      contains: "Phase 8"
    - path: "docs/canonical/sbf_training_guardrails.md"
      provides: "Updated guardrails with evidence boundary"
      contains: "local smoke"
  key_links:
    - from: "docs/canonical/sbf_validation_and_experiment_handoff.md"
      to: "scripts/train/check_active_route_train_step.py"
      via: "references the smoke script as the validation source"
      pattern: "check_active_route_train_step"
    - from: "docs/canonical/sbf_validation_and_experiment_handoff.md"
      to: "docs/canonical/sbf_semantic_first_route.md"
      via: "references the route definition and baseline"
      pattern: "support-only.*74.6"
---

<objective>
Create the validation results and experiment handoff document, and update canonical docs to reflect the evidence boundary between local smoke validation and full-train claims. Document the next full-train experiment directions with clear open questions.

Purpose: Satisfies VAL-02 (comparison baseline + next experiment direction documented) and COMP-04 (no overstatement of validation scope).
Output: New handoff document + updated canonical docs with Phase 8 validation status.
</objective>

<execution_context>
@/home/mty0201/Pointcept/semantic-boundary-field/.claude/get-shit-done/workflows/execute-plan.md
@/home/mty0201/Pointcept/semantic-boundary-field/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-local-validation-and-experiment-handoff/08-CONTEXT.md
@.planning/phases/08-local-validation-and-experiment-handoff/08-01-SUMMARY.md

<interfaces>
<!-- Canonical doc structures the executor must update. -->

From docs/canonical/sbf_semantic_first_route.md (lines 1-10):
```markdown
# Semantic-First Route Definition
## Purpose
This document defines the evidence-aligned semantic-first route selection rules for milestone v1.1.
As of Phase 7, the candidate route is implemented as the active implementation route. It is not yet full-train validated (Phase 8 scope).
```

From docs/canonical/sbf_training_guardrails.md:
- Contains training/config guardrails
- Must be updated with evidence boundary language

Smoke validation output keys (from Plan 08-01):
- pass_output_keys, pass_no_nan_losses, pass_backward_optimizer, pass_focus_activated, ALL_PASS
- evidence_boundary: local smoke validation only

Experiment directions from CONTEXT.md decisions:
- D-07: Soft masking with support_gt continuous weight (top priority)
- D-08: Negative sample calibration risk
- D-09: Alpha-sigma coupling (record only)
- D-10: Adaptive inference route (direction only)
- D-11: Give directions and open questions, do not lock hyperparameters
- D-12: Evidence boundary explicit in all validation docs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation results and experiment handoff document</name>
  <files>docs/canonical/sbf_validation_and_experiment_handoff.md</files>
  <read_first>
    - docs/canonical/sbf_semantic_first_route.md
    - docs/canonical/sbf_semantic_first_contract.md
    - docs/canonical/sbf_training_guardrails.md
    - .planning/phases/08-local-validation-and-experiment-handoff/08-CONTEXT.md
    - .planning/phases/08-local-validation-and-experiment-handoff/08-01-SUMMARY.md
    - project/losses/support_guided_semantic_focus_loss.py
    - configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py
  </read_first>
  <action>
Create `docs/canonical/sbf_validation_and_experiment_handoff.md` with the following structure and content. Per D-11, frame directions as questions to investigate, not prescriptions. Per D-12, every section that references validation must state the evidence boundary.

**Document structure:**

```markdown
# Validation And Experiment Handoff

## Purpose

This document records the Phase 8 local validation results for the support-guided semantic focus route and defines the next full-train experiment directions. It is the handoff artifact between the local implementation milestone (v1.1) and the first full-training experiment.

## Evidence Boundary

**Local validation proves:** the active route runs correctly — model forward produces `seg_logits` and `support_pred` with correct shapes, all three loss terms (`loss_semantic`, `loss_support`, `loss_focus`) are non-NaN, gradients flow via backward + optimizer step, and focus weighting activates in boundary regions (where `support_gt > 0.2`, mean `focus_weight` exceeds non-boundary regions).

**Local validation does NOT prove:** that the route improves semantic segmentation performance, that it beats the support-only baseline, or that it generalizes across the full dataset. Those claims require full-train experiments in a separate environment.

Validation script: `scripts/train/check_active_route_train_step.py`
Smoke config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train-smoke.py`

## Smoke Validation Results

| Check | Status |
|-------|--------|
| Output keys (`seg_logits`, `support_pred`) | {from 08-01-SUMMARY} |
| Non-NaN losses (semantic, support, focus) | {from 08-01-SUMMARY} |
| Backward + optimizer step | {from 08-01-SUMMARY} |
| Focus activation (boundary > non-boundary) | {from 08-01-SUMMARY} |

**Overall:** {ALL_PASS from 08-01-SUMMARY}

Note: If 08-01-SUMMARY is not yet available (plan 01 not yet executed), write placeholder rows with `[pending — run plan 08-01 first]` and a note that this section must be updated after plan 01 completes.

## Comparison Baseline

The comparison target for the next full-train experiment is:

- **Route:** support-only (reg=1, cover=0.2)
- **Metric:** val_mIoU = 74.6
- **Source:** `docs/canonical/sbf_semantic_first_route.md`

The support-guided semantic focus route must beat this baseline on val_mIoU to justify its additional complexity (focus weighting term + boundary-region semantic emphasis).

## Next Full-Train Experiment Directions

### Direction 1: Soft Masking Ablation (Top Priority)

**Question:** Does replacing the hard `valid_gt` binary mask in the support loss with a continuous `support_gt`-derived soft weight improve support prediction quality and downstream focus accuracy?

**Current implementation:** `loss_support` uses `valid_gt` as a hard binary mask — points with `valid_gt = 0` contribute zero gradient to support prediction. This is safe but potentially wasteful: points just outside the valid region still carry partial boundary proximity signal in `support_gt`.

**Ablation formula:**
```
loss_support_soft = BCE(support_logit, support_gt) * support_gt.pow(alpha)
```
where `alpha` controls how aggressively the soft mask suppresses low-proximity points.

**Open questions:**
- What range of `alpha` values produces stable training? (start with `alpha in {0.5, 1.0, 2.0}`)
- Does removing the hard `valid_gt` mask cause support prediction over-activation (false positives in non-boundary regions)?
- Is the focus weighting quality (measured by `val_boundary_mIoU` improvement over baseline) sensitive to `alpha`?

### Direction 2: Negative Sample Calibration

**Question:** When the hard `valid_gt` mask is removed (soft masking), does the support head over-predict boundary proximity in non-boundary regions?

**Risk:** Without `valid_gt` masking, the support BCE loss sees all points. If `support_gt` is near-zero for most non-boundary points, the loss landscape may still push the model toward conservative predictions, but there is a risk that the implicit negative weighting is insufficient.

**Diagnostic to implement:**
- After N epochs of soft-mask training, measure `sigmoid(support_logit)` statistics in regions where `support_gt < 0.05` — if mean prediction > 0.3, over-activation is occurring.
- Compare support_cover metric (Tversky overlap) between hard-mask and soft-mask variants.

**Open questions:**
- Does adding an explicit negative margin (e.g., `support_gt.pow(alpha) + eps` to ensure non-zero gradient everywhere) help or hurt?
- Is a two-stage approach needed (train with hard mask first, then fine-tune with soft mask)?

### Direction 3: Alpha-Sigma Coupling (Record Only)

**Observation:** The `alpha` exponent in soft masking interacts with the `sigma` parameter used to generate `support_gt` from boundary distance during preprocessing. A large `sigma` produces a broad support field; a large `alpha` then concentrates the soft mask back toward high-proximity points. These two parameters may partially cancel each other if not co-tuned.

**Open questions:**
- What is the effective support radius (`sigma` effect) after applying `support_gt.pow(alpha)` soft masking?
- Should `alpha` and `sigma` be swept jointly (grid search) or is sequential tuning sufficient?
- Is there a closed-form relationship between `sigma` and `alpha` that simplifies the search space?

This direction is recorded for future investigation. Do not implement in the current experiment cycle.

### Direction 4: Adaptive Inference Route (Future)

**Question:** At inference time, can `support_pred` (the model's own boundary proximity prediction) replace the GT-derived `focus_weight` to provide adaptive boundary emphasis without ground-truth access?

**Concept:** During training, focus weighting uses GT `support_gt` (per design note in `support_guided_semantic_focus_loss.py`). At inference, GT is unavailable. If `support_pred` is accurate enough, `focus_weight_inference = 1.0 + lambda * sigmoid(support_pred).pow(gamma)` could provide boundary-aware post-processing or confidence weighting.

**Prerequisites:** This direction only makes sense after confirming that (a) the route beats the support-only baseline and (b) `support_pred` achieves reasonable boundary coverage (measured by `support_cover` metric during full training).

This direction is documented only. Do not implement in the current experiment cycle.

## Training Config Reference

Active route train config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`
Active route smoke config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train-smoke.py`

Key hyperparameters for the first full-train run:
- `focus_lambda=1.0`, `focus_gamma=1.0` (baseline focus weighting)
- `support_loss_weight=1.0`, `focus_loss_weight=1.0`
- `seed=3407`, `total_epoch=2000`, `eval_epoch=100`
- Comparison metric: `val_mIoU` (checkpoint selected by best `val_mIoU`)

## Status

- **Phase 8 validation:** Local smoke complete (run `check_active_route_train_step.py` to reproduce)
- **Full-train experiment:** Not started — this document is the handoff
- **Next action:** Run the active route train config in a full-training environment and compare `val_mIoU` against the support-only baseline (74.6)
```

Fill in the Smoke Validation Results table from 08-01-SUMMARY if it exists. If it does not exist yet, use `[pending]` placeholders and note that the table must be updated after plan 08-01 execution.
  </action>
  <verify>
    <automated>grep "Evidence Boundary" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "does NOT prove" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "74.6" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "Soft Masking" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "Negative Sample Calibration" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "Alpha-Sigma Coupling" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "Adaptive Inference" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "check_active_route_train_step" docs/canonical/sbf_validation_and_experiment_handoff.md && grep "support_gt.pow(alpha)" docs/canonical/sbf_validation_and_experiment_handoff.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/canonical/sbf_validation_and_experiment_handoff.md`
    - File contains `## Evidence Boundary` section with "does NOT prove" language
    - File contains `## Comparison Baseline` with `74.6` and `support-only`
    - File contains `## Next Full-Train Experiment Directions` with 4 subsections
    - File contains `Direction 1: Soft Masking Ablation` marked as `(Top Priority)` per D-07
    - File contains `Direction 2: Negative Sample Calibration` with over-activation risk per D-08
    - File contains `Direction 3: Alpha-Sigma Coupling (Record Only)` per D-09
    - File contains `Direction 4: Adaptive Inference Route (Future)` per D-10
    - Directions use "Question:" framing per D-11 (not prescriptive hyperparameter values)
    - File references `check_active_route_train_step.py` as the validation script
    - File contains `support_gt.pow(alpha)` soft masking formula per specifics
  </acceptance_criteria>
  <done>Handoff document exists with evidence boundary, comparison baseline (74.6), all four experiment directions framed as questions, and no overstatement of validation scope</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical docs with Phase 8 validation status and evidence boundary</name>
  <files>docs/canonical/sbf_semantic_first_route.md, docs/canonical/sbf_training_guardrails.md</files>
  <read_first>
    - docs/canonical/sbf_semantic_first_route.md
    - docs/canonical/sbf_training_guardrails.md
    - docs/canonical/sbf_validation_and_experiment_handoff.md
  </read_first>
  <action>
**Update `docs/canonical/sbf_semantic_first_route.md`:**

1. In the `## Purpose` section, update the Phase 7 reference to reflect Phase 8 completion. Change the line:
   ```
   As of Phase 7, the candidate route is implemented as the active implementation route. It is not yet full-train validated (Phase 8 scope).
   ```
   to:
   ```
   As of Phase 8, the candidate route is implemented and locally smoke-validated. Local validation confirms the route runs correctly (non-NaN losses, gradient flow, focus activation). Full-train validation is pending — see `docs/canonical/sbf_validation_and_experiment_handoff.md` for the experiment handoff.
   ```

2. Add a new section `## Validation Status` after the `## Active Implementation Route` section (before `## Route-Selection Rules`), containing:
   ```markdown
   ## Validation Status

   - **Local smoke validation:** Complete (Phase 8). Confirms forward/backward/optimizer pass, non-NaN losses, focus activation.
   - **Full-train validation:** Not started. The comparison target is the support-only baseline (val_mIoU = 74.6).
   - **Evidence boundary:** Local validation proves "runs correctly." It does NOT prove "works well" or claim any performance improvement.
   - **Handoff:** `docs/canonical/sbf_validation_and_experiment_handoff.md`
   ```

**Update `docs/canonical/sbf_training_guardrails.md`:**

1. Read the existing content first.
2. Add a new section `## Evidence Boundary` at the end of the document (before any trailing metadata), containing:
   ```markdown
   ## Evidence Boundary

   Phase 8 local validation confirms the active route runs correctly:
   - Model forward produces `seg_logits` and `support_pred`
   - All three loss terms (`loss_semantic`, `loss_support`, `loss_focus`) are non-NaN
   - Backward pass and optimizer step succeed
   - Focus weighting activates in boundary regions

   **This is NOT full-train validation.** No performance claims are valid until the active route config is run in a full-training environment and compared against the support-only baseline (val_mIoU = 74.6). Do not cite local smoke results as evidence of improved semantic performance.

   Validation script: `scripts/train/check_active_route_train_step.py`
   Experiment handoff: `docs/canonical/sbf_validation_and_experiment_handoff.md`
   ```
  </action>
  <verify>
    <automated>grep "Phase 8" docs/canonical/sbf_semantic_first_route.md && grep "locally smoke-validated" docs/canonical/sbf_semantic_first_route.md && grep "## Validation Status" docs/canonical/sbf_semantic_first_route.md && grep "does NOT prove" docs/canonical/sbf_semantic_first_route.md && grep "## Evidence Boundary" docs/canonical/sbf_training_guardrails.md && grep "NOT full-train validation" docs/canonical/sbf_training_guardrails.md && grep "74.6" docs/canonical/sbf_training_guardrails.md</automated>
  </verify>
  <acceptance_criteria>
    - `sbf_semantic_first_route.md` contains "Phase 8" and "locally smoke-validated" in Purpose section
    - `sbf_semantic_first_route.md` contains `## Validation Status` section
    - `sbf_semantic_first_route.md` Validation Status contains "does NOT prove" evidence boundary language
    - `sbf_semantic_first_route.md` references `sbf_validation_and_experiment_handoff.md`
    - `sbf_training_guardrails.md` contains `## Evidence Boundary` section
    - `sbf_training_guardrails.md` contains "NOT full-train validation"
    - `sbf_training_guardrails.md` references `74.6` baseline
    - `sbf_training_guardrails.md` references `check_active_route_train_step.py`
    - Neither file makes performance claims about the active route
  </acceptance_criteria>
  <done>Both canonical docs updated with Phase 8 validation status and explicit evidence boundary language; no overstatement of validation scope (COMP-04)</done>
</task>

</tasks>

<verification>
1. `docs/canonical/sbf_validation_and_experiment_handoff.md` exists with all four experiment directions
2. `docs/canonical/sbf_semantic_first_route.md` updated with Phase 8 status
3. `docs/canonical/sbf_training_guardrails.md` updated with evidence boundary
4. No document claims the route improves semantic performance (grep for "improves" or "better" without qualifying language)
5. All three documents reference the 74.6 baseline or the handoff document
</verification>

<success_criteria>
- Handoff document covers all four experiment directions (D-07 through D-10)
- Directions framed as questions, not prescriptions (D-11)
- Evidence boundary explicit in all three documents (D-12, COMP-04)
- Comparison baseline (support-only 74.6) documented as the target (VAL-02)
- Cross-references between canonical docs are consistent
</success_criteria>

<output>
After completion, create `.planning/phases/08-local-validation-and-experiment-handoff/08-02-SUMMARY.md`
</output>
