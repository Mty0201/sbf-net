---
phase: 07-active-route-implementation
plan: 04
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - docs/canonical/sbf_facts.md
  - docs/canonical/sbf_semantic_first_route.md
  - docs/canonical/sbf_semantic_first_contract.md
  - docs/canonical/sbf_training_guardrails.md
  - docs/canonical/README.md
  - train.md
autonomous: true
requirements: [AUX-03, COMP-03]
must_haves:
  truths:
    - "Maintainer-facing docs describe the active implementation route as support-guided semantic focus, not as zeroed legacy field supervision."
    - "Docs clearly distinguish three categories: stable runtime entry config, strongest reference baseline, and active implementation route."
    - "Support-only remains the strongest reference baseline and support-shape remains side evidence only."
    - "No doc claims direction/distance are part of the active mainline, no doc claims full validation, no doc implies Pointcept changes."
    - "sbf_facts.md no longer frames the field route as structurally active for the current milestone."
  artifacts:
    - path: "docs/canonical/sbf_facts.md"
      provides: "Updated facts reflecting that the semantic-first route is now implemented, not just pending"
    - path: "docs/canonical/sbf_semantic_first_route.md"
      provides: "Active-route wording that distinguishes the implemented mainline from the support-only baseline"
    - path: "docs/canonical/sbf_semantic_first_contract.md"
      provides: "Contract wording aligned to the implemented semantic-plus-support route"
    - path: "docs/canonical/sbf_training_guardrails.md"
      provides: "Runtime guidance with three-category config distinction and the new active-route config"
    - path: "docs/canonical/README.md"
      provides: "Updated index reflecting the implemented route"
    - path: "train.md"
      provides: "Maintainer-facing runtime wrapper aligned to the implemented route"
  key_links:
    - from: "docs/canonical/sbf_training_guardrails.md"
      to: "configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py"
      via: "active implementation route config reference"
      pattern: "semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train"
    - from: "train.md"
      to: "docs/canonical/sbf_semantic_first_contract.md"
      via: "runtime contract link"
      pattern: "sbf_semantic_first_contract"
---

<objective>
Update all canonical docs and runtime guidance to reflect the implemented support-guided semantic focus route, with clear three-category config distinction and cross-file consistency.

Purpose: Reinforce AUX-03 and COMP-03 in documentation. Addresses MEDIUM review concerns about doc consistency risk, sbf_facts.md still framing the field route as structurally active, and the need to clearly distinguish "stable runtime entry config" from "active implementation route config."
Output: Updated canonical docs and runtime guidance with consistent, non-contradictory language.
</objective>

<execution_context>
@/home/mty0201/Pointcept/semantic-boundary-field/.claude/get-shit-done/workflows/execute-plan.md
@/home/mty0201/Pointcept/semantic-boundary-field/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-active-route-implementation/07-RESEARCH.md
@.planning/phases/07-active-route-implementation/07-01-SUMMARY.md
@.planning/phases/07-active-route-implementation/07-02-SUMMARY.md
@.planning/phases/07-active-route-implementation/07-03-SUMMARY.md
@docs/canonical/README.md
@docs/canonical/sbf_facts.md
@docs/canonical/sbf_semantic_first_route.md
@docs/canonical/sbf_semantic_first_contract.md
@docs/canonical/sbf_training_guardrails.md
@train.md

<interfaces>
The three-category distinction that MUST appear consistently across all docs:

1. **Stable runtime entry config** — `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-train.py`
   This is the stable canonical runtime entry config. It is NOT the active implementation route.
   It remains unchanged by Phase 7.

2. **Strongest reference baseline** — `support-only (reg=1, cover=0.2) = 74.6`
   Config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-support-only-train.py`
   This is the comparison target, not the active implementation route.

3. **Active implementation route** — `support-guided semantic focus route`
   Config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`
   Model: `SharedBackboneSemanticSupportModel`
   Loss: `SupportGuidedSemanticFocusLoss`
   Evaluator: `SupportGuidedSemanticFocusEvaluator`
   This is the Phase 7 active route. NOT yet full-train validated (Phase 8 scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sbf_facts.md, sbf_semantic_first_route.md, and sbf_semantic_first_contract.md</name>
  <read_first>
    - docs/canonical/sbf_facts.md
    - docs/canonical/sbf_semantic_first_route.md
    - docs/canonical/sbf_semantic_first_contract.md
    - docs/canonical/README.md
  </read_first>
  <files>docs/canonical/sbf_facts.md, docs/canonical/sbf_semantic_first_route.md, docs/canonical/sbf_semantic_first_contract.md</files>
  <action>
**sbf_facts.md changes:**

1. In `## Current Stage-2 Status`, update the bullet that says "the replacement route is still pending later milestone phases rather than already landed in runtime/config" to say: "The support-guided semantic focus route is the active implementation route as of Phase 7. It uses `SharedBackboneSemanticSupportModel`, `SupportGuidedSemanticFocusLoss`, and `SupportGuidedSemanticFocusEvaluator`. Local smoke/full-train validation is pending Phase 8."

2. In `## Current Interpretation`, update the bullet that says "Semantic-first boundary supervision is the active repository direction, but the replacement route is still pending later milestone phases" to: "The support-guided semantic focus route is now implemented as the active implementation route. Local smoke/full-train validation is pending Phase 8."

3. In `## Current Interpretation`, update the bullet about Phase 6 defining the route to: "Phase 6 defined the support-guided semantic focus route; Phase 7 implemented it using a semantic-plus-support model where support is the only explicit auxiliary boundary prediction target."

4. Keep ALL historical evidence, results, and boundary rules unchanged. Do NOT delete or modify the evidence scoreboard or source table.

**sbf_semantic_first_route.md changes:**

1. In `## Purpose`, update the second paragraph from "It does not declare a fully implemented runtime default" to: "As of Phase 7, the candidate route is implemented as the active implementation route. It is not yet full-train validated (Phase 8 scope)."

2. In `## Candidate Route`, rename section to `## Active Implementation Route` and update opening from "The Phase 6 candidate route is" to "The active implementation route is the **support-guided semantic focus route**, implemented in Phase 7." Add after the definition bullets: "Implementation: `SharedBackboneSemanticSupportModel` + `SupportGuidedSemanticFocusLoss` + `SupportGuidedSemanticFocusEvaluator`. Config: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`."

3. Keep all prohibition rules, route-selection rules, and historical reference boundary unchanged.

**sbf_semantic_first_contract.md changes:**

1. In `## Purpose`, update from "This document defines the exact Phase 6 contract" to: "This document defines the contract for the **support-guided semantic focus route**, originally defined in Phase 6 and implemented in Phase 7."

2. In `## Runtime Contract Shape`, update from "the candidate route will later receive its own repo-local loss/evaluator/config definition in Phase 7" to: "The route now has its own repo-local model, loss, evaluator, and config implemented in Phase 7: `SharedBackboneSemanticSupportModel`, `SupportGuidedSemanticFocusLoss`, `SupportGuidedSemanticFocusEvaluator`, `semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`. The stable runtime entry config remains unchanged."

3. Add a new section `## Three-Category Config Distinction` before `## Relationship To Support-Shape`:
```
## Three-Category Config Distinction

Maintainers must distinguish three config roles:

1. **Stable runtime entry config**: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-train.py` — unchanged by Phase 7.
2. **Strongest reference baseline**: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-support-only-train.py` — the comparison target, not the active route.
3. **Active implementation route**: `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py` — Phase 7 implementation.

These are intentionally separate. The stable entry config is the repository's default runtime path. The reference baseline is the evidence comparison target. The active implementation route is the new semantic-first path being evaluated.
```

4. Keep all prohibition rules and architecture boundary rules unchanged.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
facts = Path('docs/canonical/sbf_facts.md').read_text()
route = Path('docs/canonical/sbf_semantic_first_route.md').read_text()
contract = Path('docs/canonical/sbf_semantic_first_contract.md').read_text()

# Consistency checklist (addresses MEDIUM review concern)
# 1. No doc claims dir/dist are part of the active mainline
for name, text in [('facts', facts), ('route', route), ('contract', contract)]:
    assert 'active' not in text.lower() or 'direction' not in text.split('active')[-1][:200].lower() or 'no direction target' in text.lower(), f'{name} may claim dir is active'

# 2. Active route is named consistently
for name, text in [('facts', facts), ('route', route), ('contract', contract)]:
    assert 'support-guided semantic focus' in text.lower(), f'{name} missing route name'

# 3. No doc claims full validation
for name, text in [('facts', facts), ('route', route), ('contract', contract)]:
    assert 'full-train validated' not in text or 'not yet' in text or 'pending' in text, f'{name} may claim full validation'

# 4. Support-only remains baseline
for name, text in [('facts', facts), ('route', route)]:
    assert 'support-only' in text.lower() and 'baseline' in text.lower(), f'{name} missing baseline reference'

# 5. Three-category distinction in contract
assert 'stable runtime entry config' in contract.lower()
assert 'strongest reference baseline' in contract.lower() or 'reference baseline' in contract.lower()
assert 'active implementation route' in contract.lower()

# 6. sbf_facts no longer says route is pending
assert 'still pending later milestone phases rather than already landed' not in facts

# 7. Config paths present
assert 'semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train' in route or 'semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train' in contract
assert 'SharedBackboneSemanticSupportModel' in route or 'SharedBackboneSemanticSupportModel' in contract
assert 'SupportGuidedSemanticFocusLoss' in route or 'SupportGuidedSemanticFocusLoss' in contract

print('Doc consistency checklist: ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/canonical/sbf_facts.md` no longer says "replacement route is still pending later milestone phases"
    - `docs/canonical/sbf_facts.md` contains "support-guided semantic focus route" as the active implementation route
    - `docs/canonical/sbf_semantic_first_route.md` contains `## Active Implementation Route` section
    - `docs/canonical/sbf_semantic_first_route.md` contains `SharedBackboneSemanticSupportModel`
    - `docs/canonical/sbf_semantic_first_contract.md` contains `## Three-Category Config Distinction`
    - `docs/canonical/sbf_semantic_first_contract.md` contains `stable runtime entry config`, `reference baseline`, and `active implementation route`
    - All three files mention the route is not yet full-train validated
    - Historical evidence and prohibition rules remain intact in all files
  </acceptance_criteria>
  <done>Canonical route docs consistently describe the implemented mainline, preserve the three-category config distinction, and avoid claiming validation that Phase 8 has not produced.</done>
</task>

<task type="auto">
  <name>Task 2: Update sbf_training_guardrails.md, README.md, and train.md with three-category distinction</name>
  <read_first>
    - docs/canonical/sbf_training_guardrails.md
    - docs/canonical/README.md
    - train.md
  </read_first>
  <files>docs/canonical/sbf_training_guardrails.md, docs/canonical/README.md, train.md</files>
  <action>
**sbf_training_guardrails.md changes:**

1. In `## Config Roles`, restructure around the three-category distinction. Replace the current "Phase 6 semantic-first candidate route" bullet with explicit three categories:

```
- **Stable runtime entry config** (unchanged by Phase 7):
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-train.py`
  This remains the stable canonical runtime entry config for the repository.

- **Strongest reference baseline** (support-only, the comparison target):
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-support-only-train.py`
  `support-only (reg=1, cover=0.2) = 74.6`
  This is the comparison target the active route should beat. It is not the active implementation route.

- **Active implementation route** (Phase 7, support-guided semantic focus):
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`
  Uses `SharedBackboneSemanticSupportModel`, `SupportGuidedSemanticFocusLoss`, `SupportGuidedSemanticFocusEvaluator`.
  Trains from scratch (`weight = None`, `resume = False`). Not yet full-train validated (Phase 8 scope).
  Do not change `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-train.py` — it is the stable entry, not the active route.

- **Historical reference configs** (evidence only):
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-axis-side-train.py`
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-edge-axis-side-train-smoke.py`
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-semantic-train.py`
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-semantic-train-smoke.py`

- **Side evidence** (weaker than baseline):
  `support-shape` — weaker than the support-only baseline and not the semantic-first mainline.
```

2. Update the closing paragraph of `## Config Roles` from mentioning "Phase 6" to: "The runtime entrypoint and fail-fast rules stay fixed. The active implementation route is the support-guided semantic focus route. Do not rewrite the stable entry config, do not elevate support-shape to the mainline, and do not claim local smoke/full-train validation that Phase 8 has not yet produced."

3. Add a new section `## Active Route Command Pattern` after the existing `## Canonical Command Patterns`:
```
## Active Route Command Pattern

```bash
export POINTCEPT_ROOT=/path/to/Pointcept
export SBF_DATA_ROOT=/path/to/BF_edge_chunk_npy
conda run --no-capture-output -n ptv3 python scripts/train/train.py \
  --config configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py \
  --pointcept-root "${POINTCEPT_ROOT}"
```

This config trains from scratch with no checkpoint loading. No Pointcept changes required.
```

4. Keep all guardrails, invalid run patterns, and env var requirements unchanged.

**README.md changes:**

Update question 3 in the `## Four Maintainer Questions` section to reference the active implementation route instead of only the Phase 6 candidate route. Change "which candidate route is Phase 6 defining next?" to "what is the active implementation route?" and update the answer anchors to include both `sbf_semantic_first_route.md` and the contract. Add a sub-bullet mentioning the active implementation route config path.

**train.md changes:**

1. In `## 3. Current Config Roles`, update the bullet for the Phase 6 candidate route. Replace:
```
- Phase 6 semantic-first candidate route:
  `support-guided semantic focus route` documented in `docs/canonical/sbf_semantic_first_contract.md`
```
with:
```
- Active implementation route (Phase 7):
  `configs/semantic_boundary/semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train.py`
  Uses `SupportGuidedSemanticFocusLoss` and `SupportGuidedSemanticFocusEvaluator`.
  Trains from scratch. Not yet full-train validated (Phase 8 scope).
  See `docs/canonical/sbf_semantic_first_contract.md` for the route contract.
```

2. Update the closing paragraph to remove "current semantic-first design target is support-centric and support-only-first" phrasing and replace with: "The stable runtime entry remains unchanged. The support-guided semantic focus route is the active implementation route. The support-only config is the strongest reference baseline. Do not claim smoke/full-train validation that Phase 8 has not yet produced."
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
guardrails = Path('docs/canonical/sbf_training_guardrails.md').read_text()
readme = Path('docs/canonical/README.md').read_text()
train = Path('train.md').read_text()

# Three-category distinction in guardrails
assert 'Stable runtime entry config' in guardrails or 'stable runtime entry config' in guardrails
assert 'Strongest reference baseline' in guardrails or 'strongest reference baseline' in guardrails
assert 'Active implementation route' in guardrails or 'active implementation route' in guardrails
assert 'semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train' in guardrails

# No Pointcept changes claim
assert 'no Pointcept changes' in guardrails.lower() or 'No Pointcept changes' in guardrails

# train.md updated
assert 'Active implementation route' in train or 'active implementation route' in train
assert 'semseg-pt-v3m1-0-base-bf-support-guided-semantic-focus-train' in train

# No full-train validation claims
for name, text in [('guardrails', guardrails), ('train', train)]:
    lower = text.lower()
    assert 'full-train validated' not in lower or 'not yet' in lower or 'pending' in lower, f'{name} may claim validation'

# Stable entry config still referenced
assert 'semseg-pt-v3m1-0-base-bf-edge-train.py' in guardrails
assert 'scripts/train/train.py' in guardrails

# Support-only baseline still referenced
assert 'support-only' in guardrails.lower() and '74.6' in guardrails

# README references active route
assert 'active implementation route' in readme.lower() or 'support-guided semantic focus' in readme.lower()

print('Runtime guidance consistency: ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/canonical/sbf_training_guardrails.md` contains "Stable runtime entry config", "Strongest reference baseline", and "Active implementation route" as distinct categories
    - `docs/canonical/sbf_training_guardrails.md` contains the new active-route config path
    - `docs/canonical/sbf_training_guardrails.md` still contains `semseg-pt-v3m1-0-base-bf-edge-train.py` as stable entry
    - `docs/canonical/sbf_training_guardrails.md` still contains `support-only (reg=1, cover=0.2) = 74.6`
    - `docs/canonical/sbf_training_guardrails.md` contains the active route command pattern
    - `train.md` contains "Active implementation route" with the new config path
    - `train.md` does NOT claim smoke/full-train validation
    - `docs/canonical/README.md` references the active implementation route
    - None of the three files claim full-train validation has occurred
  </acceptance_criteria>
  <done>Runtime guidance consistently uses three-category config distinction, points to the new active route, preserves the stable entry and baseline, and keeps evidence boundaries accurate.</done>
</task>

</tasks>

<verification>
Run the cross-file consistency checklist (addresses MEDIUM review concern about phrase-replacement being too shallow):

```bash
python3 -c "
from pathlib import Path
files = {
    'facts': Path('docs/canonical/sbf_facts.md').read_text(),
    'route': Path('docs/canonical/sbf_semantic_first_route.md').read_text(),
    'contract': Path('docs/canonical/sbf_semantic_first_contract.md').read_text(),
    'guardrails': Path('docs/canonical/sbf_training_guardrails.md').read_text(),
    'readme': Path('docs/canonical/README.md').read_text(),
    'train': Path('train.md').read_text(),
}
errors = []

# 1. No doc claims direction/distance are part of the active mainline
for name, text in files.items():
    if 'direction' in text.lower() and 'active' in text.lower():
        # Check it's in prohibition context, not claiming it's active
        if 'no direction target' not in text.lower() and 'without' not in text.lower():
            errors.append(f'{name}: may claim direction is active')

# 2. No doc claims full validation
for name, text in files.items():
    if 'full-train validated' in text.lower() and 'not yet' not in text.lower() and 'pending' not in text.lower():
        errors.append(f'{name}: may claim full validation')

# 3. No doc implies Pointcept changes
for name, text in files.items():
    if 'pointcept change' in text.lower() and 'no pointcept change' not in text.lower() and 'no Pointcept change' not in text:
        errors.append(f'{name}: may imply Pointcept changes')

# 4. Support-only remains baseline not mainline
for name in ['facts', 'route', 'guardrails']:
    text = files[name]
    if 'support-only' in text.lower() and 'mainline' in text.lower():
        if 'not' not in text.split('support-only')[1][:100].lower():
            errors.append(f'{name}: may elevate support-only to mainline')

# 5. Active route named consistently across all files
for name, text in files.items():
    if name != 'readme':
        if 'support-guided semantic focus' not in text.lower():
            errors.append(f'{name}: missing active route name')

if errors:
    for e in errors:
        print(f'FAIL: {e}')
    raise AssertionError(f'{len(errors)} consistency check(s) failed')
print('Cross-file consistency checklist: ALL CHECKS PASSED')
"
```
</verification>

<success_criteria>
All canonical docs and runtime guidance consistently describe the support-guided semantic focus route as the active implementation route, maintain the three-category config distinction, keep support-only as baseline, and do not claim validation beyond implementation readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/07-active-route-implementation/07-04-SUMMARY.md`
</output>
