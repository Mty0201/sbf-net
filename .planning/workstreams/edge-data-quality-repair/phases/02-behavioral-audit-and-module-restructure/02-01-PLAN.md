---
phase: 02-behavioral-audit-and-module-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data_pre/bf_edge_v3/core/AUDIT_boundary_centers.md
  - data_pre/bf_edge_v3/core/AUDIT_local_clusters.md
  - data_pre/bf_edge_v3/core/AUDIT_supports.md
  - data_pre/bf_edge_v3/core/AUDIT_pointwise.md
  - data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md
autonomous: true
requirements:
  - REF-02

must_haves:
  truths:
    - "Every logical block in all 4 core modules has a behavioral classification (core/compat/infra)"
    - "All cross-stage implicit contracts are documented with field names, invariants, and risk levels"
    - "A developer can read the audit docs and distinguish algorithm from compatibility logic without reading code"
  artifacts:
    - path: "data_pre/bf_edge_v3/core/AUDIT_boundary_centers.md"
      provides: "Stage 1 behavioral audit"
      contains: "## Classification"
    - path: "data_pre/bf_edge_v3/core/AUDIT_local_clusters.md"
      provides: "Stage 2 behavioral audit"
      contains: "## Classification"
    - path: "data_pre/bf_edge_v3/core/AUDIT_supports.md"
      provides: "Stage 3 behavioral audit"
      contains: "## Classification"
    - path: "data_pre/bf_edge_v3/core/AUDIT_pointwise.md"
      provides: "Stage 4 behavioral audit"
      contains: "## Classification"
    - path: "data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md"
      provides: "Cross-stage behavioral contracts"
      contains: "## Contract"
  key_links:
    - from: "AUDIT_supports.md"
      to: "CROSS_STAGE_CONTRACTS.md"
      via: "trigger flag semantics, array index contract"
      pattern: "trigger_flag|center_index"
---

<objective>
Produce per-module behavioral audit documents and a cross-stage contracts document for the bf_edge_v3 pipeline.

Purpose: Before any code is moved or split (Plans 02-03), every logical block must be classified so that restructuring preserves behavior. This is the foundation for REF-02 ("audit and document all hidden compatibility logic") and unblocks all subsequent code changes.

Output: 4 per-module audit files (AUDIT_*.md) in core/, 1 cross-stage contracts file in docs/.
</objective>

<execution_context>
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/workflows/execute-plan.md
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/edge-data-quality-repair/ROADMAP.md
@.planning/workstreams/edge-data-quality-repair/STATE.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-CONTEXT.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-RESEARCH.md

Source files to audit:
@data_pre/bf_edge_v3/core/boundary_centers_core.py
@data_pre/bf_edge_v3/core/local_clusters_core.py
@data_pre/bf_edge_v3/core/supports_core.py
@data_pre/bf_edge_v3/core/pointwise_core.py
@data_pre/bf_edge_v3/utils/stage_io.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-module behavioral audit documents</name>
  <files>
    data_pre/bf_edge_v3/core/AUDIT_boundary_centers.md,
    data_pre/bf_edge_v3/core/AUDIT_local_clusters.md,
    data_pre/bf_edge_v3/core/AUDIT_supports.md,
    data_pre/bf_edge_v3/core/AUDIT_pointwise.md
  </files>
  <read_first>
    data_pre/bf_edge_v3/core/boundary_centers_core.py,
    data_pre/bf_edge_v3/core/local_clusters_core.py,
    data_pre/bf_edge_v3/core/supports_core.py,
    data_pre/bf_edge_v3/core/pointwise_core.py,
    .planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-RESEARCH.md
  </read_first>
  <action>
For each of the 4 core modules, create an AUDIT_{module_name}.md file in data_pre/bf_edge_v3/core/.

Each audit document must follow this structure:

```
# Behavioral Audit: {module_name}
## Module: {file_path} ({line_count} lines, {function_count} functions)

## Classification

### Core Algorithm
(blocks that would exist in any clean implementation of this stage's purpose)

For each block:
- **Lines {start}-{end}: {function_or_block_name}**
  - Purpose: {what it does}
  - Inputs: {parameters/data consumed}
  - Outputs: {values returned/modified}
  - Hardcoded parameters: {list each with exact value}

### Compatibility / Adaptation
(blocks that handle real-data edge cases, historical decisions, cross-stage workarounds)

For each block:
- **Lines {start}-{end}: {function_or_block_name}**
  - Purpose: {what real-data problem it solves}
  - Trigger condition: {when this code activates}
  - Hardcoded parameters: {list each with exact value}
  - Why it exists: {the data scenario that necessitated this}

### Infrastructure
(I/O, logging, visualization, validation, debug output)

For each block:
- **Lines {start}-{end}: {function_or_block_name}**
  - Purpose: {what infrastructure function it serves}
```

Per D-01: Granularity is per logical block (not per-line, not per-function-only). A "block" may be a function, a section within a large function, or a coherent multi-line sequence.

Per D-02: Use the three-way classification: Core algorithm, Compatibility/adaptation, Infrastructure.

Key audit findings from RESEARCH.md Section 2 to incorporate and VERIFY against the actual code:

Stage 1 (boundary_centers_core.py, 414 lines, 11 functions):
- Core: kNN graph, cross-label detection, center estimation, PCA tangent, confidence scoring
- Compat: Fallback tangent via cross product (lines ~126-144, score=0.3), empty-result zero arrays, single-point guards
- Infra: XYZ export, NPZ serialization

Stage 2 (local_clusters_core.py, 393 lines, 11 functions):
- Core: per-pair DBSCAN, per-cluster denoise (kNN+MAD), trigger metric computation
- Compat: Trigger threshold hardcodes (trigger_min_cluster_size=max(min_samples*6,48), linearity_th=0.85, tangent_coherence_th=0.88, bbox_anisotropy_th=6.0), denoise safeguards (max_remove_ratio=0.20, min_keep_points=max(min_samples,6)), conservative AND-logic for trigger
- Infra: XYZ export, NPZ serialization, cluster coloring

Stage 3 (supports_core.py, 1318 lines, 30+ functions):
- Core: fit_line_support, fit_polyline_support, build_polyline_vertices, group_tangents, compute_subgroup_metrics
- Compat (bulk of code): DEFAULT_FIT_PARAMS (20+ params), split_direction_group_into_runs, classify_trigger_subgroup, try_attach_group_to_main_bundle, should_merge_main_bundles, fragment promotion, absorb_sparse_endpoint_points, regularize_support_orientation (15deg/0.3 weight), regroup_trigger_cluster (200+ lines)
- Infra: NPZ/XYZ serialization, debug payload

Stage 4 (pointwise_core.py, 301 lines, 8 functions):
- Core: label-to-supports index, closest-point-on-segment projection, multi-segment distance, Gaussian weight
- Compat: Degenerate segment fallback (zero length -> project to start), zero-segment fallback (segment_length<=0 -> use line_start/end), legacy output aliases (edge_mask=edge_valid, edge_strength=edge_support)
- Infra: load_supports(), XYZ/NPY export

IMPORTANT: The RESEARCH.md findings are a starting point. You MUST read each source file and verify/correct line numbers, parameter values, and function names. Add any blocks the research missed.

For supports_core.py (the 53KB primary target): Be especially thorough. Document every function in the trigger path. List every parameter in DEFAULT_FIT_PARAMS with its exact default value.
  </action>
  <verify>
    <automated>grep -c "## Classification" data_pre/bf_edge_v3/core/AUDIT_boundary_centers.md data_pre/bf_edge_v3/core/AUDIT_local_clusters.md data_pre/bf_edge_v3/core/AUDIT_supports.md data_pre/bf_edge_v3/core/AUDIT_pointwise.md && grep -c "### Core Algorithm" data_pre/bf_edge_v3/core/AUDIT_supports.md && grep -c "### Compatibility" data_pre/bf_edge_v3/core/AUDIT_supports.md</automated>
  </verify>
  <acceptance_criteria>
    - Each AUDIT_*.md contains exactly one "## Classification" heading
    - Each AUDIT_*.md contains "### Core Algorithm", "### Compatibility / Adaptation", "### Infrastructure" sections
    - AUDIT_supports.md lists every parameter from DEFAULT_FIT_PARAMS with its exact default value
    - AUDIT_supports.md documents regroup_trigger_cluster and all sub-functions in the trigger path
    - AUDIT_local_clusters.md lists trigger_min_cluster_size, linearity_th=0.85, tangent_coherence_th=0.88, bbox_anisotropy_th=6.0
    - AUDIT_pointwise.md documents edge_mask/edge_strength legacy aliases
    - All line number references verified against actual source files
  </acceptance_criteria>
  <done>All 4 AUDIT_*.md files exist with complete per-block three-way classification, verified line numbers, and all hardcoded parameters listed with exact values</done>
</task>

<task type="auto">
  <name>Task 2: Cross-stage behavioral contracts document</name>
  <files>
    data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md
  </files>
  <read_first>
    data_pre/bf_edge_v3/core/AUDIT_boundary_centers.md,
    data_pre/bf_edge_v3/core/AUDIT_local_clusters.md,
    data_pre/bf_edge_v3/core/AUDIT_supports.md,
    data_pre/bf_edge_v3/core/AUDIT_pointwise.md,
    data_pre/bf_edge_v3/utils/stage_io.py,
    data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py,
    .planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-RESEARCH.md
  </read_first>
  <action>
Create data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md documenting all implicit behavioral contracts between stages. Per D-07, these are the cross-stage behavioral contracts that must become explicit.

Structure:

```
# Cross-Stage Behavioral Contracts

## Overview
(brief: what this document captures and why it matters for refactoring)

## Contract 1: Stage 2 -> Stage 3 Trigger Flag Semantics
- **Producer:** local_clusters_core.cluster_boundary_centers()
- **Consumer:** supports_core.build_supports_payload() / regroup_trigger_cluster()
- **Field:** cluster_trigger_flag
- **Invariant:** trigger_flag > 0 means cluster contains multiple edge branches requiring direction-based regrouping
- **Hidden assumption:** tangent vectors in flagged clusters are meaningful enough for direction grouping
- **Risk:** Medium — if trigger criteria change, Stage 3 regrouping may receive ungroupable clusters
- **Validation rule:** {exact check that could enforce this}

## Contract 2: Stage 1 -> Stage 3 Array Index Contract
- **Producer:** boundary_centers_core.build_boundary_centers()
- **Consumer:** supports_core (indexes Stage 1 arrays via center_index from Stage 2)
- **Fields:** center_index from Stage 2 used as index into Stage 1's center_coord, center_tangent, confidence
- **Invariant:** all values in center_index are valid indices into Stage 1 arrays (0 <= idx < len(center_coord))
- **Hidden assumption:** Stage 1 arrays never reordered/filtered between output and Stage 3 consumption
- **Risk:** Low if NPZ preserved; High if intermediate format changes
- **Validation rule:** {exact check}

## Contract 3: Stage 3 -> Stage 4 Support Schema Contract
- **Producer:** supports_core.build_supports_payload()
- **Consumer:** pointwise_core.closest_points_to_support() / build_pointwise_edge_supervision()
- **Fields:** segment_offset, segment_length, segment_start, segment_end, line_start, line_end, semantic_pair
- **Invariant:** every support has either valid segments (segment_length > 0) OR valid line endpoints
- **Hidden assumption:** no support exists with neither segments nor line endpoints
- **Risk:** Medium — violation produces infinite distances silently
- **Validation rule:** {exact check}

## Contract 4: Stage 3 Internal Spacing-Adaptive Thresholds
- **Producer:** estimate_local_spacing(coords, k=6)
- **Consumer:** all trigger regrouping thresholds in supports_core
- **Invariant:** local_spacing > 0 (clamped to EPS if fewer than k points)
- **Hidden assumption:** clusters have >= 6 points for reliable spacing estimate
- **Risk:** Low (safeguarded by EPS clamp)

## Contract 5: In-Memory vs On-Disk Equivalence
- **Producer:** build_support_dataset_v3.py (in-memory pipeline)
- **Consumer:** per-scene scripts (on-disk NPZ pipeline)
- **Invariant:** identical core function calls with identical arguments
- **Hidden assumption:** NPZ serialization is lossless for float32/int32
- **Divergence points:** {list exact function signature differences between the two paths, found by reading both}
- **Risk:** Low for correctness; Medium for maintainability

## NPZ Schema Summary
(For each intermediate file, list all field names, shapes, and dtypes that downstream stages depend on)

### boundary_centers.npz
| Field | Shape | Dtype | Consumed by |
|-------|-------|-------|-------------|
| center_coord | (M, 3) | float32 | Stage 2, Stage 3 |
| center_normal | (M, 3) | float32 | (unused downstream) |
| center_tangent | (M, 3) | float32 | Stage 2, Stage 3 |
| semantic_pair | (M, 2) | int32 | Stage 2 |
| source_point_index | (M,) | int32 | (unused downstream) |
| confidence | (M,) | float32 | Stage 3 |

### local_clusters.npz
{same format — verify exact fields from stage_io.py load functions and Stage 3 consumption}

### supports.npz
{same format — verify exact fields from Stage 4 consumption}
```

Per D-07: These contracts must be explicit enough that any refactoring change can be checked against them.

IMPORTANT: Verify all field names, shapes, and dtypes by reading the actual save/load code in stage_io.py and the core modules. Do NOT rely solely on RESEARCH.md — it may have omissions. Check build_support_dataset_v3.py for divergence points between in-memory and on-disk paths.
  </action>
  <verify>
    <automated>grep -c "## Contract" data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md && grep "boundary_centers.npz\|local_clusters.npz\|supports.npz" data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - CROSS_STAGE_CONTRACTS.md contains at least 5 "## Contract" sections
    - Document includes NPZ Schema Summary with tables for boundary_centers.npz, local_clusters.npz, and supports.npz
    - Each contract lists Producer, Consumer, Fields, Invariant, Hidden assumption, Risk, Validation rule
    - Contract 5 lists specific divergence points between build_support_dataset_v3.py and per-scene scripts
    - All field names verified against actual save/load code (not just RESEARCH.md assumptions)
  </acceptance_criteria>
  <done>CROSS_STAGE_CONTRACTS.md exists with all 5+ contracts fully documented, NPZ schemas verified against code, and in-memory vs on-disk divergence points enumerated</done>
</task>

</tasks>

<verification>
All audit and contract documents exist and contain the required structure:
- 4 AUDIT_*.md files with three-way classification per block
- 1 CROSS_STAGE_CONTRACTS.md with 5+ contracts and NPZ schema tables
- grep for "## Classification" returns 1 per audit file
- grep for "## Contract" returns >= 5 in contracts file
</verification>

<success_criteria>
- Every logical block in all 4 core modules is classified as core/compat/infra
- All hardcoded parameters listed with exact values
- All cross-stage implicit contracts documented with invariants and risks
- NPZ schemas documented with field-level consumer tracking
- Documents are detailed enough for Plan 02 executor to restructure code without re-reading entire source
</success_criteria>

<output>
After completion, create `.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-01-SUMMARY.md`
</output>
