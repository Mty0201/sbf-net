---
phase: 02-behavioral-audit-and-module-restructure
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - data_pre/bf_edge_v3/core/fitting.py
  - data_pre/bf_edge_v3/core/trigger_regroup.py
  - data_pre/bf_edge_v3/core/supports_export.py
  - data_pre/bf_edge_v3/core/supports_core.py
  - data_pre/bf_edge_v3/scripts/fit_local_supports.py
  - data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py
autonomous: true
requirements: [REF-01, REF-03]
must_haves:
  truths:
    - "supports_core.py is decomposed into fitting.py, trigger_regroup.py, supports_export.py, and slimmed supports_core.py"
    - "Each sub-module has a single clear responsibility matching RESEARCH.md Section 5.1 boundaries"
    - "Compatibility logic lives in trigger_regroup.py, separated from core fitting in fitting.py"
    - "Both invocation paths work: per-scene scripts AND build_support_dataset_v3.py"
    - "All imports resolve and no circular dependencies exist"
  artifacts:
    - path: "data_pre/bf_edge_v3/core/fitting.py"
      provides: "Core fitting algorithms: fit_line_support, fit_polyline_support, geometry primitives"
      exports: ["fit_line_support", "fit_polyline_support", "build_polyline_vertices", "regularize_support_orientation", "point_to_line_distance"]
    - path: "data_pre/bf_edge_v3/core/trigger_regroup.py"
      provides: "Trigger cluster regrouping compatibility logic"
      exports: ["regroup_trigger_cluster", "group_tangents", "split_direction_group_into_runs"]
    - path: "data_pre/bf_edge_v3/core/supports_export.py"
      provides: "Support NPZ/XYZ export and visualization"
      exports: ["export_npz"]
    - path: "data_pre/bf_edge_v3/core/supports_core.py"
      provides: "Slimmed orchestration: build_supports_payload and support record assembly"
      exports: ["build_supports_payload"]
  key_links:
    - from: "data_pre/bf_edge_v3/core/supports_core.py"
      to: "data_pre/bf_edge_v3/core/fitting.py"
      via: "import"
      pattern: "from .fitting import|from fitting import"
    - from: "data_pre/bf_edge_v3/core/supports_core.py"
      to: "data_pre/bf_edge_v3/core/trigger_regroup.py"
      via: "import"
      pattern: "from .trigger_regroup import|from trigger_regroup import"
    - from: "data_pre/bf_edge_v3/scripts/fit_local_supports.py"
      to: "data_pre/bf_edge_v3/core/supports_core.py"
      via: "import build_supports_payload"
      pattern: "build_supports_payload"
---

<objective>
Decompose supports_core.py (53.6KB, 1318 lines, 30+ functions) into 4 focused sub-modules per RESEARCH.md Section 5.1 boundaries.

Purpose: This is the primary structural win of Phase 2 (per RESEARCH.md: "Stage 3 is where refactor pays off most"). The trigger path is 6:1 compatibility-to-core ratio -- separating it enables independent evolution. Satisfies REF-01 (modular stages with clear I/O) and REF-03 (separate core from compat at module boundaries).

Output: 4 files replacing the monolithic supports_core.py -- fitting.py (core algorithms), trigger_regroup.py (compatibility logic), supports_export.py (I/O), slimmed supports_core.py (orchestration). Both per-scene and dataset invocation paths verified working.
</objective>

<execution_context>
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/workflows/execute-plan.md
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/edge-data-quality-repair/ROADMAP.md
@.planning/workstreams/edge-data-quality-repair/STATE.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-CONTEXT.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-RESEARCH.md
@data_pre/bf_edge_v3/core/BEHAVIORAL_AUDIT.md (from Plan 01 -- classification of all blocks)
@data_pre/bf_edge_v3/core/CROSS_STAGE_CONTRACTS.md (from Plan 01 -- cross-stage contracts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract fitting.py and trigger_regroup.py from supports_core.py</name>
  <files>
    data_pre/bf_edge_v3/core/fitting.py,
    data_pre/bf_edge_v3/core/trigger_regroup.py,
    data_pre/bf_edge_v3/core/supports_core.py
  </files>
  <read_first>
    - data_pre/bf_edge_v3/core/supports_core.py (FULL read -- this is the file being decomposed, 53.6KB)
    - data_pre/bf_edge_v3/core/BEHAVIORAL_AUDIT.md (Stage 3 section -- tells you which blocks go where)
    - data_pre/bf_edge_v3/core/CROSS_STAGE_CONTRACTS.md (Stage 3 internal contract on spacing-adaptive thresholds)
    - data_pre/bf_edge_v3/utils/common.py (shared utilities: EPS, normalize_vector, normalize_rows)
  </read_first>
  <action>
**CRITICAL CONSTRAINT: No behavioral changes. Every function must produce identical output for identical input. Move code, do not rewrite it.**

**Step 1: Create fitting.py (~200 lines)**

Move these functions from supports_core.py to fitting.py (all classified [CORE] or [COMPAT] in BEHAVIORAL_AUDIT.md):
- `point_to_line_distance()` -- geometry primitive
- `fit_line_support()` -- core line fitting via PCA
- `build_polyline_vertices()` -- polyline vertex construction
- `fit_polyline_support()` -- core polyline fitting
- `regularize_support_orientation()` -- [COMPAT] axis-aligned snap (15-deg threshold, 0.3 weight)
- `estimate_local_spacing()` -- used by both fitting and trigger paths
- Any geometry helper functions these depend on (check imports within each function)

Header: Copy the exact same imports each function needs from supports_core.py. Add module docstring: "Core fitting algorithms for support element generation. Extracted from supports_core.py during Phase 2 refactor. All functions preserve original behavior exactly."

**Step 2: Create trigger_regroup.py (~600 lines)**

Move these functions (all classified [COMPAT] or [MIXED] in BEHAVIORAL_AUDIT.md):
- `group_tangents()` -- [CORE] but only used by trigger path
- `split_direction_group_into_runs()` -- [COMPAT]
- `compute_subgroup_metrics()` -- [CORE] but only used by trigger path
- `classify_trigger_subgroup()` -- [COMPAT]
- `try_attach_group_to_main_bundle()` -- [COMPAT]
- `should_merge_main_bundles()` -- [COMPAT]
- `absorb_sparse_endpoint_points()` -- [COMPAT]
- `regroup_trigger_cluster()` -- [MIXED] master orchestration of trigger path

Header: Import fitting functions FROM fitting.py (e.g., `from .fitting import estimate_local_spacing, fit_line_support, fit_polyline_support` or the non-package equivalent matching the existing import pattern in the codebase). Add module docstring: "Trigger cluster regrouping -- compatibility/adaptation logic for handling clusters where DBSCAN produced mixed-direction groupings. Extracted from supports_core.py during Phase 2 refactor."

**Step 3: Slim down supports_core.py**

After extraction, supports_core.py should contain only:
- `rebuild_cluster_records()` -- cluster record reconstruction
- `build_standard_support_record()` -- non-trigger support assembly
- `build_trigger_support_records()` -- trigger support assembly (calls into trigger_regroup)
- `build_support_record()` -- dispatch between standard and trigger paths
- `build_supports_payload()` -- top-level orchestration + NPZ assembly
- `DEFAULT_FIT_PARAMS` -- keep here temporarily (Plan 03 extracts to params.py)

Update imports in supports_core.py:
- Add: `from .fitting import fit_line_support, fit_polyline_support, regularize_support_orientation, estimate_local_spacing` (or non-package equivalent matching existing pattern)
- Add: `from .trigger_regroup import regroup_trigger_cluster` (or non-package equivalent)
- Remove: function definitions that were moved out

**Import pattern:** Check how existing core modules import from each other and from utils/. If they use relative imports (`from .module import`), use that. If they use `_bootstrap.py` sys.path manipulation, use the same pattern. Do NOT introduce a new import mechanism.

**Verification during implementation:** After each file is created, verify that every function that was in the original supports_core.py is now in exactly one of the 4 files (fitting.py, trigger_regroup.py, supports_export.py from Task 2, or slimmed supports_core.py). No function should be duplicated. No function should be lost.
  </action>
  <verify>
    <automated>cd /home/mty0201/Pointcept/sbf-net && python -c "
import sys; sys.path.insert(0, 'data_pre/bf_edge_v3/core')
import fitting; import trigger_regroup; import supports_core
# Verify key exports exist
assert hasattr(fitting, 'fit_line_support'), 'fitting.fit_line_support missing'
assert hasattr(fitting, 'fit_polyline_support'), 'fitting.fit_polyline_support missing'
assert hasattr(trigger_regroup, 'regroup_trigger_cluster'), 'trigger_regroup.regroup_trigger_cluster missing'
assert hasattr(supports_core, 'build_supports_payload'), 'supports_core.build_supports_payload missing'
print('PASS: All key functions importable from correct modules')
"</automated>
  </verify>
  <acceptance_criteria>
    - fitting.py exists with fit_line_support, fit_polyline_support, build_polyline_vertices, regularize_support_orientation, estimate_local_spacing
    - trigger_regroup.py exists with regroup_trigger_cluster, group_tangents, split_direction_group_into_runs, compute_subgroup_metrics, classify_trigger_subgroup, try_attach_group_to_main_bundle, should_merge_main_bundles, absorb_sparse_endpoint_points
    - supports_core.py retains ONLY orchestration: build_supports_payload, build_support_record, build_standard_support_record, build_trigger_support_records, rebuild_cluster_records, DEFAULT_FIT_PARAMS
    - All 3 files import successfully with no ImportError
    - No function is duplicated across files
    - No function from original supports_core.py is missing
    - DEFAULT_FIT_PARAMS values are byte-identical to original (exact same literal values)
  </acceptance_criteria>
  <done>supports_core.py is split into fitting.py (~200 lines core algorithms), trigger_regroup.py (~600 lines compatibility logic), and slimmed supports_core.py (~300 lines orchestration). All imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Extract supports_export.py and update all call sites</name>
  <files>
    data_pre/bf_edge_v3/core/supports_export.py,
    data_pre/bf_edge_v3/scripts/fit_local_supports.py,
    data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py
  </files>
  <read_first>
    - data_pre/bf_edge_v3/core/supports_core.py (read the slimmed version from Task 1)
    - data_pre/bf_edge_v3/scripts/fit_local_supports.py (per-scene entry point for Stage 3)
    - data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py (in-memory pipeline -- MUST also work after refactor)
    - data_pre/bf_edge_v3/core/CROSS_STAGE_CONTRACTS.md (in-memory bypass contract)
  </read_first>
  <action>
**Step 1: Create supports_export.py (~200 lines)**

Move all [INFRA]-classified export/visualization functions from supports_core.py:
- `export_npz()` or equivalent NPZ serialization function
- `export_support_geometry_xyz()` or equivalent XYZ visualization export
- `export_trigger_group_classes_xyz()` or equivalent trigger visualization
- Any other debug/visualization helper functions classified as [INFRA] in BEHAVIORAL_AUDIT.md

Module docstring: "Support element I/O and visualization export. Extracted from supports_core.py during Phase 2 refactor."

**Step 2: Update fit_local_supports.py (per-scene script)**

This script imports from supports_core. Update its imports to reference the new module structure. The key function it calls is `build_supports_payload()` which stays in supports_core.py, so the import may not change -- but verify. If it also directly calls any function that moved to fitting.py, trigger_regroup.py, or supports_export.py, update those imports.

Also check if `build_runtime_params()` in this script references DEFAULT_FIT_PARAMS -- if so, the import path must still work.

**Step 3: Update build_support_dataset_v3.py (in-memory pipeline)**

This script runs Stages 1-3 in-memory. It may directly call functions that have moved. Read the file and update every import that now points to a different module. Per CROSS_STAGE_CONTRACTS.md in-memory bypass contract: this script must continue to work identically.

Specifically check:
- Does it import build_supports_payload? (stays in supports_core.py)
- Does it import any fitting functions directly? (now in fitting.py)
- Does it import any export functions directly? (now in supports_export.py)
- Does it duplicate build_runtime_params()? (per RESEARCH.md P7)

Update imports to match new module locations. Do NOT change any function call signatures or parameter values.

**Step 4: Update __init__.py**

If data_pre/bf_edge_v3/core/__init__.py exports from supports_core, update to also export from new sub-modules (or re-export via supports_core for backward compatibility).
  </action>
  <verify>
    <automated>cd /home/mty0201/Pointcept/sbf-net && python -c "
import sys; sys.path.insert(0, 'data_pre/bf_edge_v3/core')
import supports_export
print('supports_export imports OK')
# Verify script-level imports work
sys.path.insert(0, 'data_pre/bf_edge_v3/scripts')
sys.path.insert(0, 'data_pre/bf_edge_v3')
# Check that the main entry functions are reachable
from supports_core import build_supports_payload
print('build_supports_payload reachable from supports_core')
print('PASS: All modules and scripts importable')
"</automated>
  </verify>
  <acceptance_criteria>
    - supports_export.py exists with NPZ serialization and XYZ visualization functions
    - fit_local_supports.py imports resolve without errors
    - build_support_dataset_v3.py imports resolve without errors
    - No function signature changed (only import paths updated)
    - build_supports_payload() is still callable from supports_core module
    - __init__.py updated if it previously exported supports_core functions
  </acceptance_criteria>
  <done>All 4 sub-modules created (fitting.py, trigger_regroup.py, supports_export.py, slimmed supports_core.py). Both invocation paths (per-scene scripts and build_support_dataset_v3.py) have updated imports and resolve without errors.</done>
</task>

</tasks>

<verification>
- 4 new/modified files: fitting.py, trigger_regroup.py, supports_export.py, supports_core.py (slimmed)
- All imports resolve: `python -c "from supports_core import build_supports_payload"` succeeds
- No function duplicated or lost
- fit_local_supports.py and build_support_dataset_v3.py import successfully
- DEFAULT_FIT_PARAMS values unchanged
- No NPZ schema changes (field names, shapes, dtypes identical)
</verification>

<success_criteria>
supports_core.py is decomposed into 4 focused modules. Core fitting algorithms (fitting.py) are separated from compatibility logic (trigger_regroup.py) at module boundaries per REF-03. Both invocation paths work per guardrail 6. The 4-stage pipeline shape is preserved per D-05.
</success_criteria>

<output>
After completion, create `.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-02-SUMMARY.md`
</output>
