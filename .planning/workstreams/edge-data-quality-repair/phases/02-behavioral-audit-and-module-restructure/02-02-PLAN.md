---
phase: 02-behavioral-audit-and-module-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - data_pre/bf_edge_v3/core/fitting.py
  - data_pre/bf_edge_v3/core/trigger_regroup.py
  - data_pre/bf_edge_v3/core/supports_export.py
  - data_pre/bf_edge_v3/core/supports_core.py
autonomous: true
requirements:
  - REF-01
  - REF-03

must_haves:
  truths:
    - "supports_core.py is decomposed into fitting.py, trigger_regroup.py, supports_export.py, and a slimmed supports_core.py"
    - "Compatibility logic in trigger_regroup.py is clearly separated from core fitting in fitting.py"
    - "All existing public function signatures from supports_core.py remain importable (via re-exports or same module)"
    - "All 4 stages still execute without import errors when run on test scenes"
  artifacts:
    - path: "data_pre/bf_edge_v3/core/fitting.py"
      provides: "Core fitting algorithms: fit_line_support, fit_polyline_support, geometry primitives"
      contains: "def fit_line_support"
    - path: "data_pre/bf_edge_v3/core/trigger_regroup.py"
      provides: "Trigger cluster regrouping: regroup_trigger_cluster and all sub-functions"
      contains: "def regroup_trigger_cluster"
    - path: "data_pre/bf_edge_v3/core/supports_export.py"
      provides: "NPZ/XYZ export and visualization helpers"
      contains: "def export"
    - path: "data_pre/bf_edge_v3/core/supports_core.py"
      provides: "Slimmed orchestration: build_supports_payload, build_support_record"
      contains: "def build_supports_payload"
  key_links:
    - from: "data_pre/bf_edge_v3/core/supports_core.py"
      to: "data_pre/bf_edge_v3/core/fitting.py"
      via: "import fit_line_support, fit_polyline_support"
      pattern: "from .fitting import|from fitting import"
    - from: "data_pre/bf_edge_v3/core/supports_core.py"
      to: "data_pre/bf_edge_v3/core/trigger_regroup.py"
      via: "import regroup_trigger_cluster"
      pattern: "from .trigger_regroup import|from trigger_regroup import"
---

<objective>
Decompose supports_core.py (1318 lines, 30+ functions) into 4 focused modules per RESEARCH.md Section 5.1, and separate compatibility logic from core algorithm per D-06.

Purpose: Stage 3 is 60%+ of effort (RESEARCH guardrail #4). The trigger regrouping path (600+ lines) is almost entirely compatibility logic interleaved with core fitting. Splitting into fitting.py (core) and trigger_regroup.py (compat) makes each independently understandable, testable, and evolvable. This directly delivers REF-01 (modular stages) and REF-03 (core vs compat separation).

Output: 4 files replacing the monolithic supports_core.py. The slimmed supports_core.py imports from the three extracted modules.
</objective>

<execution_context>
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/workflows/execute-plan.md
@/home/mty0201/Pointcept/sbf-net/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/edge-data-quality-repair/ROADMAP.md
@.planning/workstreams/edge-data-quality-repair/STATE.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-CONTEXT.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-RESEARCH.md
@.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-01-SUMMARY.md

Source files:
@data_pre/bf_edge_v3/core/supports_core.py
@data_pre/bf_edge_v3/core/AUDIT_supports.md
@data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md
@data_pre/bf_edge_v3/scripts/fit_local_supports.py
@data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract fitting.py and trigger_regroup.py from supports_core.py</name>
  <files>
    data_pre/bf_edge_v3/core/fitting.py,
    data_pre/bf_edge_v3/core/trigger_regroup.py,
    data_pre/bf_edge_v3/core/supports_export.py,
    data_pre/bf_edge_v3/core/supports_core.py
  </files>
  <read_first>
    data_pre/bf_edge_v3/core/supports_core.py,
    data_pre/bf_edge_v3/core/AUDIT_supports.md,
    data_pre/bf_edge_v3/docs/CROSS_STAGE_CONTRACTS.md,
    data_pre/bf_edge_v3/scripts/fit_local_supports.py,
    data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py
  </read_first>
  <action>
Split supports_core.py into 4 modules following RESEARCH.md Section 5.1 and the AUDIT_supports.md classification. Per D-04, separate clustering from refinement/splitting. Per D-05, keep the 4-stage pipeline shape — these are internal sub-modules under Stage 3. Per D-06, compatibility logic goes into clearly marked sections/functions within trigger_regroup.py.

**CRITICAL A/B BOUNDARY RULE: No default parameter values may change. No algorithm behavior may change. This is structure-only.**

**fitting.py** (~200 lines) — Core fitting algorithms (classified "Core Algorithm" in audit):
- fit_line_support() — PCA-based line fitting
- fit_polyline_support() — PCA-ordered vertex downsampling
- build_polyline_vertices() — vertex construction helper
- regularize_support_orientation() — axis-aligned direction snapping (15deg threshold, 0.3 weight). NOTE: this is classified as "Compatibility" in the audit but is tightly coupled to fitting output. Place it here with a clear comment: "# COMPATIBILITY: architectural facade prior — see AUDIT_supports.md"
- Geometry primitives: point_to_line_distance, point_to_segment_distance, project_point_to_segment, and any other geometry helpers used by fitting
- All imports these functions need (numpy, scipy, etc.)

**trigger_regroup.py** (~600 lines) — Trigger cluster regrouping (classified "Compatibility/Adaptation" in audit):
- group_tangents() — sign-invariant tangent direction clustering
- split_direction_group_into_runs() — spatial run segmentation
- compute_subgroup_metrics() — PCA quality metrics for subgroups
- classify_trigger_subgroup() — three-way classification: main/fragment/bad
- try_attach_group_to_main_bundle() — fragment-to-main attachment
- should_merge_main_bundles() — 6-condition merge check
- absorb_sparse_endpoint_points() — endpoint scavenging
- regroup_trigger_cluster() — master orchestration of trigger path
- estimate_local_spacing() — local density estimation helper (used by trigger thresholds)
- Any helper functions called exclusively by the above
- Header comment: "# Trigger cluster regrouping — compatibility/adaptation logic for handling\n# clusters where DBSCAN produced mixed-direction groupings.\n# See AUDIT_supports.md for behavioral classification of each function."

**supports_export.py** (~200 lines) — Infrastructure:
- All NPZ export functions (export_npz or equivalent)
- All XYZ visualization export functions
- export_support_geometry_xyz, export_trigger_group_classes_xyz
- Debug payload construction helpers
- Any visualization/coloring utility functions

**supports_core.py** (slimmed, ~300 lines) — Orchestration + assembly:
- DEFAULT_FIT_PARAMS dict — keep here for now (Phase 3 will move to config)
- rebuild_cluster_records() — cluster reconstruction from NPZ data
- build_standard_support_record() — non-trigger support creation
- build_trigger_support_records() — trigger support creation (calls into trigger_regroup)
- build_support_record() — dispatch between standard and trigger paths
- build_supports_payload() — top-level Stage 3 entry point
- Import from fitting, trigger_regroup, and supports_export
- Re-export key functions so existing callers (fit_local_supports.py, build_support_dataset_v3.py) continue working without import path changes

**Import strategy for backward compatibility:**
The slimmed supports_core.py must import and re-export all functions that are currently imported by external callers. Check fit_local_supports.py and build_support_dataset_v3.py for their exact import statements from supports_core, and ensure all those names remain importable from supports_core.

**How to verify no function is lost:**
1. Before splitting, grep supports_core.py for all "def " lines — this is the complete function inventory
2. After splitting, verify every function appears in exactly one of the 4 new files
3. Run: python -c "from data_pre.bf_edge_v3.core.supports_core import build_supports_payload" (or equivalent import check via script entry points)

**Parameter preservation:**
- DEFAULT_FIT_PARAMS values must be copied EXACTLY (same literal strings, same float values, same dict structure)
- No rounding, no reformatting of numeric literals
- If DEFAULT_FIT_PARAMS contains 0.85, the new file must contain 0.85, not 0.850 or 0.85000
  </action>
  <verify>
    <automated>cd /home/mty0201/Pointcept/sbf-net && python -c "
import sys; sys.path.insert(0, 'data_pre/bf_edge_v3')
from core.fitting import fit_line_support, fit_polyline_support
from core.trigger_regroup import regroup_trigger_cluster, group_tangents
from core.supports_export import *
from core.supports_core import build_supports_payload, DEFAULT_FIT_PARAMS
print('All imports OK')
print(f'fitting.py functions: fit_line_support, fit_polyline_support')
print(f'trigger_regroup.py functions: regroup_trigger_cluster, group_tangents')
print(f'DEFAULT_FIT_PARAMS keys: {len(DEFAULT_FIT_PARAMS)}')
"</automated>
  </verify>
  <acceptance_criteria>
    - fitting.py exists and contains def fit_line_support and def fit_polyline_support
    - trigger_regroup.py exists and contains def regroup_trigger_cluster, def group_tangents, def split_direction_group_into_runs, def classify_trigger_subgroup, def try_attach_group_to_main_bundle, def should_merge_main_bundles, def absorb_sparse_endpoint_points
    - supports_export.py exists and contains at least one def export function
    - supports_core.py (slimmed) contains def build_supports_payload and DEFAULT_FIT_PARAMS
    - supports_core.py imports from fitting, trigger_regroup, and supports_export
    - All function names from original supports_core.py appear in exactly one of the 4 files
    - DEFAULT_FIT_PARAMS values are identical to original (byte-level comparison of the dict literal)
    - python import check passes: all 4 modules importable, build_supports_payload callable
  </acceptance_criteria>
  <done>supports_core.py decomposed into 4 modules; all functions accounted for; imports work; DEFAULT_FIT_PARAMS values unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update all callers and verify Stage 3 still runs</name>
  <files>
    data_pre/bf_edge_v3/scripts/fit_local_supports.py,
    data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py
  </files>
  <read_first>
    data_pre/bf_edge_v3/scripts/fit_local_supports.py,
    data_pre/bf_edge_v3/scripts/build_support_dataset_v3.py,
    data_pre/bf_edge_v3/core/supports_core.py
  </read_first>
  <action>
Update all script files that import from supports_core to work with the new module structure. Per RESEARCH guardrail #6, BOTH invocation paths must work.

1. Read fit_local_supports.py and build_support_dataset_v3.py to find all import statements referencing supports_core
2. If supports_core.py re-exports everything properly (from Task 1), imports may already work unchanged. Verify this.
3. If any imports fail, update the import statements in the scripts to point to the correct new module (fitting, trigger_regroup, or supports_export). Prefer updating supports_core.py re-exports over changing script imports, to minimize blast radius.
4. Check for any other files that import from supports_core:
   - grep -r "from.*supports_core" data_pre/bf_edge_v3/
   - grep -r "import supports_core" data_pre/bf_edge_v3/
5. Update any discovered additional importers.

Per RESEARCH guardrail #6 and CONTEXT D-05: Both per-scene scripts AND build_support_dataset_v3.py must work after refactor.

Verification: Run a dry import test that loads all script entry points without error.
  </action>
  <verify>
    <automated>cd /home/mty0201/Pointcept/sbf-net && python -c "
import sys; sys.path.insert(0, 'data_pre/bf_edge_v3')
# Test per-scene script imports
exec(open('data_pre/bf_edge_v3/scripts/fit_local_supports.py').read().split('if __name__')[0])
print('fit_local_supports.py imports OK')
" 2>&1 | head -5 && python -c "
import sys; sys.path.insert(0, 'data_pre/bf_edge_v3')
from core.supports_core import build_supports_payload
from core.fitting import fit_line_support
from core.trigger_regroup import regroup_trigger_cluster
print('All module imports verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - fit_local_supports.py can be imported without error
    - build_support_dataset_v3.py can be imported without error
    - No file outside data_pre/bf_edge_v3/core/ has a broken import to supports_core
    - grep -r "from.*supports_core import" data_pre/bf_edge_v3/scripts/ shows only names that supports_core.py re-exports
  </acceptance_criteria>
  <done>All callers updated; both per-scene and dataset invocation paths import cleanly; no broken references to old supports_core layout</done>
</task>

</tasks>

<verification>
Stage 3 decomposition complete:
- supports_core.py (1318 lines) split into fitting.py + trigger_regroup.py + supports_export.py + slimmed supports_core.py
- All callers updated and importing cleanly
- No function lost (inventory check)
- DEFAULT_FIT_PARAMS values byte-identical to original
- Per D-06: compatibility logic is now in trigger_regroup.py, clearly separated from core fitting in fitting.py
</verification>

<success_criteria>
- 4 focused modules exist replacing monolithic supports_core.py
- Compatibility logic (trigger path) is isolated in trigger_regroup.py
- Core fitting algorithms are isolated in fitting.py
- All existing callers work without modification (or with minimal import updates)
- No algorithm behavior changed (structure-only refactor)
- Both invocation paths (per-scene + dataset) functional
</success_criteria>

<output>
After completion, create `.planning/workstreams/edge-data-quality-repair/phases/02-behavioral-audit-and-module-restructure/02-02-SUMMARY.md`
</output>
